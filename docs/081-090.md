# GomsBook AI Interview 081~090 상세판

> 주제: AI Agent · MCP · Prompt · 평가

## 081. AI Agent란?

### 답변

목표에 따라 Tool과 행동을 선택·실행하는 시스템입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 목표에 따라 Tool과 행동을 선택·실행하는 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 082. Tool Calling이란?

### 답변

LLM이 필요한 외부 기능과 Arguments를 구조화해 호출하도록 하는 방식입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> LLM이 필요한 외부 기능과 Arguments를 구조화해 호출하도록 하는 방식입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 083. Function Calling과 Tool Calling의 차이는?

### 답변

Function Calling은 함수 호출에 초점, Tool Calling은 검색·API·DB 등 더 넓은 외부 능력을 포함할 수 있습니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Function Calling은 함수 호출에 초점, Tool Calling은 검색·API·DB 등 더 넓은 외부 능력을 포함할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 두 방법을 실제 문제에서 선택한다면 어떤 기준으로 선택하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 084. Agent Loop란?

### 답변

판단→Action→Tool→Observation→재판단을 반복하는 구조입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 판단→Action→Tool→Observation→재판단을 반복하는 구조입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 085. Planning이란?

### 답변

복잡한 목표를 단계로 나누고 실행 순서를 결정하는 과정입니다.

### 상세 설명

핵심은 '복잡한 목표를 단계로 나누고 실행 순서를 결정하는 과정입니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> 복잡한 목표를 단계로 나누고 실행 순서를 결정하는 과정입니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 086. Agent Memory란?

### 답변

이전 상호작용이나 작업 상태를 이후 판단에 활용하는 메커니즘입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 이전 상호작용이나 작업 상태를 이후 판단에 활용하는 메커니즘입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 087. MCP란?

### 답변

AI 애플리케이션이 Tools/Resources/Prompts와 표준 방식으로 연결되는 Model Context Protocol입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> AI 애플리케이션이 Tools/Resources/Prompts와 표준 방식으로 연결되는 Model Context Protocol입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 088. Agent와 MCP 차이는?

### 답변

Agent는 판단·실행 로직, MCP는 연결 프로토콜입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Agent는 판단·실행 로직, MCP는 연결 프로토콜입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 두 방법을 실제 문제에서 선택한다면 어떤 기준으로 선택하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 089. RAG와 Agent 차이는?

### 답변

RAG는 지식 검색 패턴, Agent는 행동과 Tool을 결정·실행하는 시스템입니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> RAG는 지식 검색 패턴, Agent는 행동과 Tool을 결정·실행하는 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 두 방법을 실제 문제에서 선택한다면 어떤 기준으로 선택하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 090. Agentic RAG란?

### 답변

Agent가 Retrieval 과정의 의사결정을 수행하는 구조입니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> Agent가 Retrieval 과정의 의사결정을 수행하는 구조입니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 검색 성능이 낮다면 가장 먼저 어떤 지표와 실패 사례를 확인하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---
