# GomsBook AI Interview 231~240 상세판

> 주제: Agent 기본 · RAG/MCP

## 231. Prompt Engineering이란?

### 답변

Role, Instruction, Context, Constraints, Output Format, Examples 등을 설계해 모델 행동을 유도하는 방법입니다.

### 상세 설명

핵심은 'Role, Instruction, Context, Constraints, Output Format, Examples 등을 설계해 모델 행동을 유도하는 방법입니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> Role, Instruction, Context, Constraints, Output Format, Examples 등을 설계해 모델 행동을 유도하는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 232. Zero-shot과 Few-shot의 차이는?

### 답변

Zero-shot은 예시 없이 지시만 제공하고 Few-shot은 소수의 입력-출력 예시를 Prompt에 포함합니다.

### 상세 설명

핵심은 'Zero-shot은 예시 없이 지시만 제공하고 Few-shot은 소수의 입력-출력 예시를 Prompt에 포함합니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> Zero-shot은 예시 없이 지시만 제공하고 Few-shot은 소수의 입력-출력 예시를 Prompt에 포함합니다.

### 꼬리질문

**Q. 두 방법을 실제 문제에서 선택한다면 어떤 기준으로 선택하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 233. In-Context Learning이란?

### 답변

Parameter를 업데이트하지 않고 Prompt의 예시와 Context에서 작업 패턴을 추론해 수행하는 능력입니다.

### 상세 설명

핵심은 'Parameter를 업데이트하지 않고 Prompt의 예시와 Context에서 작업 패턴을 추론해 수행하는 능력입니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> Parameter를 업데이트하지 않고 Prompt의 예시와 Context에서 작업 패턴을 추론해 수행하는 능력입니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 234. Temperature와 Top-p 차이는?

### 답변

Temperature는 전체 확률분포의 날카로움을 조절하고 Top-p는 누적 확률 기준으로 Sampling 후보 집합을 제한합니다.

### 상세 설명

핵심은 'Temperature는 전체 확률분포의 날카로움을 조절하고 Top-p는 누적 확률 기준으로 Sampling 후보 집합을 제한합니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> Temperature는 전체 확률분포의 날카로움을 조절하고 Top-p는 누적 확률 기준으로 Sampling 후보 집합을 제한합니다.

### 꼬리질문

**Q. 두 방법을 실제 문제에서 선택한다면 어떤 기준으로 선택하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 235. Structured Output이 중요한 이유는?

### 답변

JSON/Schema 등 정해진 구조로 결과를 받아 프로그램이 안정적으로 파싱·검증·후속 처리할 수 있기 때문입니다.

### 상세 설명

핵심은 'JSON/Schema 등 정해진 구조로 결과를 받아 프로그램이 안정적으로 파싱·검증·후속 처리할 수 있기 때문입니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> JSON/Schema 등 정해진 구조로 결과를 받아 프로그램이 안정적으로 파싱·검증·후속 처리할 수 있기 때문입니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 236. Function Calling이란?

### 답변

LLM이 실행할 함수와 Arguments를 선택하고 Application Runtime이 실제 함수를 실행해 결과를 모델에 돌려주는 방식입니다.

### 상세 설명

핵심은 'LLM이 실행할 함수와 Arguments를 선택하고 Application Runtime이 실제 함수를 실행해 결과를 모델에 돌려주는 방식입니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> LLM이 실행할 함수와 Arguments를 선택하고 Application Runtime이 실제 함수를 실행해 결과를 모델에 돌려주는 방식입니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 237. AI Agent란?

### 답변

LLM에 Tool, Memory/Context, 실행 제어를 결합해 목표에 따라 여러 단계의 행동을 수행하는 시스템입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> LLM에 Tool, Memory/Context, 실행 제어를 결합해 목표에 따라 여러 단계의 행동을 수행하는 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 238. Tool Calling이란?

### 답변

Agent/LLM이 Tool Registry의 외부 기능 중 필요한 Tool과 Arguments를 선택해 Runtime에 실행을 요청하는 메커니즘입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Agent/LLM이 Tool Registry의 외부 기능 중 필요한 Tool과 Arguments를 선택해 Runtime에 실행을 요청하는 메커니즘입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 239. MCP란?

### 답변

Model Context Protocol로 AI 애플리케이션과 외부 Tool·Resource·Prompt 제공자를 표준 방식으로 연결하는 프로토콜입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Model Context Protocol로 AI 애플리케이션과 외부 Tool·Resource·Prompt 제공자를 표준 방식으로 연결하는 프로토콜입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 240. Agent + RAG 구조를 설명해보세요.

### 답변

Agent가 목표를 보고 Retrieval 필요 여부와 Tool을 결정하며 RAG는 Agent가 사용할 수 있는 지식 검색 능력 중 하나입니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> Agent가 목표를 보고 Retrieval 필요 여부와 Tool을 결정하며 RAG는 Agent가 사용할 수 있는 지식 검색 능력 중 하나입니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 검색 성능이 낮다면 가장 먼저 어떤 지표와 실패 사례를 확인하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---
