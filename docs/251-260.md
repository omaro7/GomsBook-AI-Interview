# GomsBook AI Interview 251~260 상세판

> 주제: Agentic Workflow · Multi-Agent · Security

## 251. AI Agent와 Workflow의 차이는 무엇인가요?

### 답변

Workflow는 실행 경로가 주로 코드로 사전 정의되고 Agent는 현재 상태와 Observation을 바탕으로 다음 행동을 동적으로 선택할 수 있습니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Workflow는 실행 경로가 주로 코드로 사전 정의되고 Agent는 현재 상태와 Observation을 바탕으로 다음 행동을 동적으로 선택할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 두 방법을 실제 문제에서 선택한다면 어떤 기준으로 선택하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 252. Deterministic Workflow와 Agentic Workflow 차이는?

### 답변

Deterministic은 코드가 경로를 결정하고 Agentic은 모델이 일부 의사결정을 수행하며 실전에서는 deterministic control과 agentic decision을 결합할 수 있습니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Deterministic은 코드가 경로를 결정하고 Agentic은 모델이 일부 의사결정을 수행하며 실전에서는 deterministic control과 agentic decision을 결합할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 두 방법을 실제 문제에서 선택한다면 어떤 기준으로 선택하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 253. Agent Orchestration이란?

### 답변

Agent, LLM, Tool, RAG, Memory를 연결하고 상태, retry, timeout, approval, 종료조건 등 전체 실행 흐름을 관리하는 것입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Agent, LLM, Tool, RAG, Memory를 연결하고 상태, retry, timeout, approval, 종료조건 등 전체 실행 흐름을 관리하는 것입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 254. Multi-Agent System이란?

### 답변

역할이 다른 여러 Agent가 작업을 분담·협력하여 하나의 목표를 수행하는 시스템입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 역할이 다른 여러 Agent가 작업을 분담·협력하여 하나의 목표를 수행하는 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 255. Multi-Agent는 서로 어떻게 통신하나요?

### 답변

자연어 또는 구조화된 Message, Shared State, 중앙 Orchestrator 등을 통해 Task, Status, Result를 교환할 수 있습니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 자연어 또는 구조화된 Message, Shared State, 중앙 Orchestrator 등을 통해 Task, Status, Result를 교환할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 256. Supervisor Agent란?

### 답변

여러 Worker Agent 중 어떤 Agent에게 어떤 작업을 맡길지 판단하고 결과를 취합하는 상위 조정 Agent입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 여러 Worker Agent 중 어떤 Agent에게 어떤 작업을 맡길지 판단하고 결과를 취합하는 상위 조정 Agent입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 257. AI Agent에서 Security가 중요한 이유는?

### 답변

Agent는 File, API, DB, Tool 등에 실제 Action을 수행할 수 있어 잘못된 판단이나 공격이 외부 시스템 변경으로 이어질 수 있기 때문입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> Agent는 File, API, DB, Tool 등에 실제 Action을 수행할 수 있어 잘못된 판단이나 공격이 외부 시스템 변경으로 이어질 수 있기 때문입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 258. Prompt Injection이란?

### 답변

악의적인 입력이 모델의 원래 지시나 정책보다 우선하도록 유도해 의도하지 않은 응답이나 행동을 일으키려는 공격입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 악의적인 입력이 모델의 원래 지시나 정책보다 우선하도록 유도해 의도하지 않은 응답이나 행동을 일으키려는 공격입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 259. RAG Prompt Injection이란?

### 답변

검색된 외부 문서 안의 악의적 지시가 LLM Context에 들어가 Agent 행동을 조작하는 Indirect Prompt Injection 위험입니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> 검색된 외부 문서 안의 악의적 지시가 LLM Context에 들어가 Agent 행동을 조작하는 Indirect Prompt Injection 위험입니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 검색 성능이 낮다면 가장 먼저 어떤 지표와 실패 사례를 확인하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 260. AI Agent 연구의 주요 한계는?

### 답변

신뢰성, 평가 어려움, 보안, 비용·Latency, 장기 작업의 오류 누적과 통제 가능성이 주요 한계입니다.

### 상세 설명

Agent 시스템에서는 LLM의 판단과 실제 실행을 분리해야 합니다. LLM은 다음 행동이나 Tool을 선택할 수 있지만 실제 실행, 권한 검증, timeout, retry, 승인 같은 제어는 Runtime 계층에서 결정론적으로 관리하는 것이 안전합니다.

### 면접 핵심

> 신뢰성, 평가 어려움, 보안, 비용·Latency, 장기 작업의 오류 누적과 통제 가능성이 주요 한계입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 Agent Runtime과 Tool 실행 계층을 분리하고, EPUB 변경처럼 부작용이 있는 작업은 승인(HITL)과 검증을 거쳐 실행하는 구조로 연결할 수 있습니다.

### 꼬리질문

**Q. 이 기능을 LLM에게 맡기지 않고 코드로 강제해야 하는 부분은 무엇입니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---
