# GomsBook AI Interview 201~210 상세판

> 주제: Retrieval 심화

## 201. Retriever의 역할은?

### 답변

Query와 관련성이 높은 문서 또는 Chunk를 찾아 LLM에 제공할 후보 Context를 선택하는 것입니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> Query와 관련성이 높은 문서 또는 Chunk를 찾아 LLM에 제공할 후보 Context를 선택하는 것입니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 202. Embedding은 Retrieval에서 어떤 역할을 하나요?

### 답변

Query와 Document를 같은 Vector 공간에 표현해 의미 유사도 기반 검색을 가능하게 합니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> Query와 Document를 같은 Vector 공간에 표현해 의미 유사도 기반 검색을 가능하게 합니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 검색 성능이 낮다면 가장 먼저 어떤 지표와 실패 사례를 확인하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 203. Cosine Similarity란?

### 답변

두 Vector의 각도 기반 유사도로 (A·B)/(||A||||B||)이며 방향이 비슷할수록 값이 큽니다.

### 상세 설명

핵심은 '두 Vector의 각도 기반 유사도로 (A·B)/(||A||||B||)이며 방향이 비슷할수록 값이 큽니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 핵심 공식

`cos(A,B) = (A·B) / (||A|| ||B||)`

### 면접 핵심

> 두 Vector의 각도 기반 유사도로 (A·B)/(||A||||B||)이며 방향이 비슷할수록 값이 큽니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 204. Top-K는 어떻게 정하나요?

### 답변

작으면 Recall이 떨어지고 크면 Noise와 비용이 늘어 동일 평가셋에서 성능을 비교해 정합니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> 작으면 Recall이 떨어지고 크면 Noise와 비용이 늘어 동일 평가셋에서 성능을 비교해 정합니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 205. Similarity Threshold란?

### 답변

유사도 점수가 기준보다 낮은 검색 결과를 제외하는 cutoff이며 모델·데이터별로 튜닝해야 합니다.

### 상세 설명

핵심은 '유사도 점수가 기준보다 낮은 검색 결과를 제외하는 cutoff이며 모델·데이터별로 튜닝해야 합니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> 유사도 점수가 기준보다 낮은 검색 결과를 제외하는 cutoff이며 모델·데이터별로 튜닝해야 합니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 206. Reranking이란?

### 답변

빠른 1차 검색으로 후보를 얻은 뒤 더 정밀한 모델이나 기준으로 후보 순위를 다시 계산하는 단계입니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> 빠른 1차 검색으로 후보를 얻은 뒤 더 정밀한 모델이나 기준으로 후보 순위를 다시 계산하는 단계입니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 207. Chunking 전략의 Trade-off는?

### 답변

작은 Chunk는 검색 정밀도가 높지만 문맥이 잘릴 수 있고 큰 Chunk는 문맥은 풍부하지만 검색 신호가 희석될 수 있습니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> 작은 Chunk는 검색 정밀도가 높지만 문맥이 잘릴 수 있고 큰 Chunk는 문맥은 풍부하지만 검색 신호가 희석될 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 검색 성능이 낮다면 가장 먼저 어떤 지표와 실패 사례를 확인하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 208. Parent-Child Context Expansion이란?

### 답변

작은 Child Chunk로 정밀 검색한 뒤 Parent나 인접 Chunk까지 확장해 LLM에 충분한 문맥을 제공하는 방법입니다.

### 상세 설명

핵심은 '작은 Child Chunk로 정밀 검색한 뒤 Parent나 인접 Chunk까지 확장해 LLM에 충분한 문맥을 제공하는 방법입니다.'라는 정의를 암기하는 데서 끝나지 않고, 이 개념이 어떤 문제를 해결하며 인접 개념과 어떻게 다른지 설명하는 것입니다.

### 면접 핵심

> 작은 Child Chunk로 정밀 검색한 뒤 Parent나 인접 Chunk까지 확장해 LLM에 충분한 문맥을 제공하는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 209. Hybrid Search란?

### 답변

Dense Semantic Retrieval과 Sparse Keyword/BM25 Retrieval을 결합하는 검색 방식입니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> Dense Semantic Retrieval과 Sparse Keyword/BM25 Retrieval을 결합하는 검색 방식입니다.

### 꼬리질문

**Q. 이 개념의 장점뿐 아니라 한계나 실패 사례도 설명해보세요.**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---

## 210. RAG 개선 실험은 어떻게 설계하나요?

### 답변

Baseline을 고정하고 한 요소씩 변경해 동일 Dataset과 Metrics로 비교하는 Ablation 방식이 적합합니다.

### 상세 설명

RAG에서는 검색 단계와 생성 단계를 분리해서 보는 것이 중요합니다. 검색 품질이 낮으면 LLM이 좋은 생성 능력을 가져도 근거 기반 답변의 안정성이 떨어질 수 있습니다.

### 면접 핵심

> Baseline을 고정하고 한 요소씩 변경해 동일 Dataset과 Metrics로 비교하는 Ablation 방식이 적합합니다.

### GomsBook 프로젝트 연결

GomsBook에서는 EPUB/XHTML을 검색 대상으로 삼아 Project-scoped RAG를 구성하고, Retrieval과 Generation을 분리 평가하는 방식으로 이 개념을 실제 검증할 수 있습니다.

### 꼬리질문

**Q. 검색 성능이 낮다면 가장 먼저 어떤 지표와 실패 사례를 확인하겠습니까?**

답변할 때는 정의를 반복하기보다 선택 기준, Trade-off, 실패 원인, 평가 방법까지 연결해 설명합니다.

---
