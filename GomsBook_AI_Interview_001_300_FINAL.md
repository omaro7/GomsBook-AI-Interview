# GomsBook AI Interview Q&A 001\~300 --- Final Detailed Edition

# GomsBook AI Interview 001\~010 상세판

> 주제: AI/ML 기초 · Transformer · 학습

## 001. 인공지능(AI)이란 무엇인가요?

### 답변

인간의 인식·추론·학습·의사결정 같은 지능적 작업을 컴퓨터가 수행하도록
하는 기술 분야입니다.

### 상세 설명

AI는 하나의 알고리즘을 뜻하지 않습니다. 규칙 기반 시스템, 머신러닝,
딥러닝, 생성형 AI처럼 다양한 접근을 포괄합니다. 면접에서는 AI ⊃ ML ⊃
DL의 포함 관계를 함께 설명하면 개념 경계가 명확해집니다.

### 면접 핵심

> 인간의 인식·추론·학습·의사결정 같은 지능적 작업을 컴퓨터가 수행하도록
> 하는 기술 분야입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 002. 머신러닝과 딥러닝의 차이는?

### 답변

머신러닝은 데이터에서 패턴을 학습하는 방법의 총칭이고, 딥러닝은 다층
신경망을 사용하는 머신러닝의 한 분야입니다.

### 상세 설명

전통적 머신러닝은 사람이 특징을 설계하는 경우가 많고, 딥러닝은 신경망이
데이터에서 계층적 표현을 함께 학습하는 데 강점이 있습니다. 데이터
규모·연산량·설명가능성·문제 유형에 따라 선택이 달라집니다.

### 면접 핵심

> 머신러닝은 데이터에서 패턴을 학습하는 방법의 총칭이고, 딥러닝은 다층
> 신경망을 사용하는 머신러닝의 한 분야입니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 003. 지도학습·비지도학습·강화학습의 차이는?

### 답변

지도학습은 Label로 학습하고, 비지도학습은 Label 없이 구조를 찾으며,
강화학습은 환경과 상호작용하며 보상을 최대화합니다.

### 상세 설명

세 방식의 핵심 차이는 학습 신호입니다. 지도학습은 정답 Label,
비지도학습은 데이터 자체의 구조, 강화학습은 환경에서 받은 Reward를
사용합니다.

### 면접 핵심

> 지도학습은 Label로 학습하고, 비지도학습은 Label 없이 구조를 찾으며,
> 강화학습은 환경과 상호작용하며 보상을 최대화합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 004. 신경망이란 무엇인가요?

### 답변

Weight·Bias·Activation을 가진 계산 단위를 여러 층으로 연결하고 Loss를
Backpropagation으로 최소화하는 모델입니다.

### 상세 설명

한 뉴런은 보통 z=w\^Tx+b를 계산한 뒤 Activation을 적용합니다. 여러
Layer를 쌓아 복잡한 함수를 표현하고, Forward 결과의 Loss를
Backpropagation으로 미분해 Weight를 갱신합니다.

### 면접 핵심

> Weight·Bias·Activation을 가진 계산 단위를 여러 층으로 연결하고 Loss를
> Backpropagation으로 최소화하는 모델입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 005. Transformer란 무엇인가요?

### 답변

Attention을 중심으로 시퀀스를 처리하여 토큰 관계를 직접 계산하고 병렬
학습에 유리한 신경망 구조입니다.

### 상세 설명

Transformer는 recurrence 대신 Attention을 사용해 멀리 떨어진 Token 사이
관계를 직접 계산합니다. 학습 시 병렬화가 쉽지만 표준 Self-Attention은
Sequence 길이에 대해 O(n²) 비용을 갖는다는 한계가 있습니다.

### 면접 핵심

> Attention을 중심으로 시퀀스를 처리하여 토큰 관계를 직접 계산하고 병렬
> 학습에 유리한 신경망 구조입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 006. Self-Attention이란?

### 답변

같은 시퀀스의 토큰들이 서로의 관련성을 계산해 문맥적 표현을 만드는
방법입니다.

### 상세 설명

입력 표현에서 Query, Key, Value를 만들고 Query-Key 유사도로 각 Value의
가중치를 정합니다. 같은 시퀀스 내부에서 계산하므로 Self-Attention이라고
부릅니다.

### 면접 핵심

> 같은 시퀀스의 토큰들이 서로의 관련성을 계산해 문맥적 표현을 만드는
> 방법입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 007. LLM이란 무엇인가요?

### 답변

대규모 데이터와 파라미터로 언어 패턴을 학습한 모델이며 대표적인
Decoder-only LLM은 다음 Token을 예측하며 생성합니다.

### 상세 설명

LLM은 대규모 Corpus에서 Token의 조건부 확률을 학습합니다. Decoder-only
모델은 P(x_t\|x\_\<t)를 반복 계산하여 한 Token씩 생성합니다. 지식을
완벽히 저장한 데이터베이스가 아니므로 Hallucination과 최신성 문제가
존재합니다.

### 면접 핵심

> 대규모 데이터와 파라미터로 언어 패턴을 학습한 모델이며 대표적인
> Decoder-only LLM은 다음 Token을 예측하며 생성합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 008. Embedding이란?

### 답변

텍스트·이미지 등을 의미적 특징을 반영하는 고차원 Vector로 표현하는
방법입니다.

### 상세 설명

Embedding의 핵심은 의미적으로 비슷한 입력이 Vector 공간에서도 가깝도록
표현하는 것입니다. RAG에서는 Document와 Query를 같은 Embedding 공간으로
보내 Cosine Similarity나 Dot Product로 후보를 찾습니다.

### 면접 핵심

> 텍스트·이미지 등을 의미적 특징을 반영하는 고차원 Vector로 표현하는
> 방법입니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 009. RAG란 무엇인가요?

### 답변

외부 문서를 검색해 관련 Context를 LLM에 제공한 뒤 근거 기반 답변을
생성하는 Retrieval-Augmented Generation입니다.

### 상세 설명

RAG는 먼저 외부 Knowledge Base에서 관련 Context를 검색하고, 그 Context를
Prompt에 추가한 뒤 LLM이 답을 생성합니다. 모델 Parameter를 다시 학습하지
않고도 변경되는 지식을 반영할 수 있다는 점이 Fine-tuning과 큰
차이입니다.

### 면접 핵심

> 외부 문서를 검색해 관련 Context를 LLM에 제공한 뒤 근거 기반 답변을
> 생성하는 Retrieval-Augmented Generation입니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 010. AI Agent란 무엇인가요?

### 답변

목표를 이해하고 LLM 판단을 바탕으로 Tool과 외부 시스템을 선택·실행하는
시스템입니다.

### 상세 설명

Agent는 단순 Chat Completion보다 한 단계 넓은 실행 시스템입니다. LLM이
판단을 담당하고 Runtime이 Tool 실행·상태·반복·오류·권한을 관리합니다.
따라서 Agent의 품질은 답변 품질뿐 아니라 행동의 정확성과 안전성까지
포함합니다.

### 면접 핵심

> 목표를 이해하고 LLM 판단을 바탕으로 Tool과 외부 시스템을 선택·실행하는
> 시스템입니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 011\~020 상세판

> 주제: AI/ML 기초 · Transformer · 학습

## 011. Loss Function이란?

### 답변

예측과 정답의 차이를 수치화한 함수이며 학습은 일반적으로 이를
최소화합니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 면접 핵심

> 예측과 정답의 차이를 수치화한 함수이며 학습은 일반적으로 이를
> 최소화합니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 012. MSE란?

### 답변

예측값과 실제값 차이의 제곱 평균으로 MSE=(1/N)Σ(y-ŷ)²입니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\mathrm{MSE}=\frac{1}{N}\sum_{i=1}^{N}(y_i-\hat{y}_i)^2
$$

### 면접 핵심

> 예측값과 실제값 차이의 제곱 평균으로 MSE=(1/N)Σ(y-ŷ)²입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 013. Cross Entropy란?

### 답변

실제 정답 분포와 모델 예측 확률분포의 차이를 측정하는 분류용 Loss입니다.

### 상세 설명

정답 클래스에 높은 확률을 줄수록 Loss가 작아지고, 정답 확률이 0에
가까워질수록 큰 패널티를 받습니다. Softmax와 함께 쓰면 Logit에 대한
Gradient가 p-y로 단순화되어 분류와 LLM next-token training에 자연스럽게
연결됩니다.

### 핵심 수식

$$
\mathcal{L}_{CE}=-\sum_{i=1}^{C}y_i\log p_i
$$

### 면접 핵심

> 실제 정답 분포와 모델 예측 확률분포의 차이를 측정하는 분류용
> Loss입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 014. Binary와 Multi-class Classification의 차이는?

### 답변

Binary는 두 클래스, Multi-class는 여러 상호배타적 클래스 중 하나를
예측합니다.

### 상세 설명

Binary는 두 클래스, Multi-class는 여러 상호배타적 클래스 중 하나를
예측합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Binary는 두 클래스, Multi-class는 여러 상호배타적 클래스 중 하나를
> 예측합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 015. Softmax란?

### 답변

여러 Logit을 합이 1인 확률분포로 변환하는 함수입니다.

### 상세 설명

Softmax는 Logit의 상대적 크기를 확률로 바꿉니다. 수치 안정성을 위해 실제
구현에서는 모든 Logit에서 max(z)를 뺀 뒤 exp를 계산하는 방식이
일반적입니다.

### 핵심 수식

$$
\mathrm{softmax}(z_i)=\frac{e^{z_i}}{\sum_{j=1}^{C}e^{z_j}}
$$

### 면접 핵심

> 여러 Logit을 합이 1인 확률분포로 변환하는 함수입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 016. Backpropagation이란?

### 답변

Chain Rule로 Loss에서 각 파라미터까지 Gradient를 역방향으로 계산하는
알고리즘입니다.

### 상세 설명

Chain Rule로 Loss에서 각 파라미터까지 Gradient를 역방향으로 계산하는
알고리즘입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Chain Rule로 Loss에서 각 파라미터까지 Gradient를 역방향으로 계산하는
> 알고리즘입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 017. Gradient Descent란?

### 답변

Gradient 반대 방향으로 파라미터를 갱신해 Loss를 줄이는 최적화
방법입니다.

### 상세 설명

Gradient는 Loss가 가장 빠르게 증가하는 방향이므로 그 반대 방향으로
이동합니다. 학습률 η가 너무 크면 최소점을 지나치거나 발산하고, 너무
작으면 수렴이 느립니다.

### 핵심 수식

$$
\theta_{t+1}=\theta_t-\eta\nabla_\theta L(\theta_t)
$$

### 면접 핵심

> Gradient 반대 방향으로 파라미터를 갱신해 Loss를 줄이는 최적화
> 방법입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 018. SGD와 Mini-batch의 차이는?

### 답변

SGD는 좁은 의미에서 한 샘플, Mini-batch는 작은 데이터 묶음으로
Gradient를 계산합니다.

### 상세 설명

SGD는 좁은 의미에서 한 샘플, Mini-batch는 작은 데이터 묶음으로
Gradient를 계산합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계,
인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> SGD는 좁은 의미에서 한 샘플, Mini-batch는 작은 데이터 묶음으로
> Gradient를 계산합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 019. Adam Optimizer란?

### 답변

Gradient의 1·2차 모멘트 이동평균을 이용해 파라미터별 업데이트를 조정하는
Optimizer입니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 면접 핵심

> Gradient의 1·2차 모멘트 이동평균을 이용해 파라미터별 업데이트를
> 조정하는 Optimizer입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 020. Fine-tuning이란?

### 답변

사전학습 모델을 특정 데이터·작업에 맞게 추가 학습하는 과정입니다.

### 상세 설명

사전학습 모델을 특정 데이터·작업에 맞게 추가 학습하는 과정입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 사전학습 모델을 특정 데이터·작업에 맞게 추가 학습하는 과정입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 021\~030 상세판

> 주제: 신경망 · 평가 · Regularization

## 021. 활성화 함수가 왜 필요한가요?

### 답변

신경망에 비선형성을 추가해 복잡한 함수를 학습할 수 있게 합니다.

### 상세 설명

신경망에 비선형성을 추가해 복잡한 함수를 학습할 수 있게 합니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 신경망에 비선형성을 추가해 복잡한 함수를 학습할 수 있게 합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 022. ReLU란?

### 답변

f(x)=max(0,x)이며 계산이 단순하지만 Dying ReLU 문제가 생길 수 있습니다.

### 상세 설명

f(x)=max(0,x)이며 계산이 단순하지만 Dying ReLU 문제가 생길 수 있습니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> f(x)=max(0,x)이며 계산이 단순하지만 Dying ReLU 문제가 생길 수
> 있습니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 023. Sigmoid의 문제점은?

### 답변

포화 영역에서 Gradient가 매우 작아져 Vanishing Gradient를 유발할 수
있습니다.

### 상세 설명

포화 영역에서 Gradient가 매우 작아져 Vanishing Gradient를 유발할 수
있습니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 포화 영역에서 Gradient가 매우 작아져 Vanishing Gradient를 유발할 수
> 있습니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 024. Vanishing Gradient란?

### 답변

역전파 중 Gradient가 매우 작아져 앞쪽 Layer가 제대로 학습되지 않는
현상입니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 면접 핵심

> 역전파 중 Gradient가 매우 작아져 앞쪽 Layer가 제대로 학습되지 않는
> 현상입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 025. CNN이란?

### 답변

Convolution으로 지역적·공간적 특징을 추출하는 신경망입니다.

### 상세 설명

Convolution으로 지역적·공간적 특징을 추출하는 신경망입니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> Convolution으로 지역적·공간적 특징을 추출하는 신경망입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 026. RNN이란?

### 답변

이전 Hidden State를 다음 시점에 전달하며 순차 데이터를 처리하는
신경망입니다.

### 상세 설명

이전 Hidden State를 다음 시점에 전달하며 순차 데이터를 처리하는
신경망입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 이전 Hidden State를 다음 시점에 전달하며 순차 데이터를 처리하는
> 신경망입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 027. LSTM과 RNN의 차이는?

### 답변

LSTM은 Cell State와 Gate로 장기 의존성 문제를 완화합니다.

### 상세 설명

LSTM은 Cell State와 Gate로 장기 의존성 문제를 완화합니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> LSTM은 Cell State와 Gate로 장기 의존성 문제를 완화합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 028. RNN/LSTM과 Transformer의 차이는?

### 답변

RNN은 순차 처리, Transformer는 Self-Attention으로 토큰 관계를 직접
계산합니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> RNN은 순차 처리, Transformer는 Self-Attention으로 토큰 관계를 직접
> 계산합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 029. Precision과 Recall의 차이는?

### 답변

Precision=TP/(TP+FP), Recall=TP/(TP+FN)입니다.

### 상세 설명

Precision=TP/(TP+FP), Recall=TP/(TP+FN)입니다. 면접에서는 이 정의에 더해
적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
\mathrm{Precision}=\frac{TP}{TP+FP},\qquad \mathrm{Recall}=\frac{TP}{TP+FN}
$$

### 면접 핵심

> Precision=TP/(TP+FP), Recall=TP/(TP+FN)입니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 030. F1 Score란?

### 답변

Precision과 Recall의 조화평균으로 F1=2PR/(P+R)입니다.

### 상세 설명

Precision과 Recall의 조화평균으로 F1=2PR/(P+R)입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 핵심 수식

$$
F_1=2\frac{PR}{P+R}
$$

### 면접 핵심

> Precision과 Recall의 조화평균으로 F1=2PR/(P+R)입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 031\~040 상세판

> 주제: 신경망 · 평가 · Regularization

## 031. Bias와 Variance란?

### 답변

High Bias는 Underfitting, High Variance는 Overfitting과 연결됩니다.

### 상세 설명

High Bias는 Underfitting, High Variance는 Overfitting과 연결됩니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> High Bias는 Underfitting, High Variance는 Overfitting과 연결됩니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 032. Regularization이란?

### 답변

Overfitting을 줄여 일반화 성능을 높이는 방법입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> Overfitting을 줄여 일반화 성능을 높이는 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 033. L1과 L2의 차이는?

### 답변

L1은 \|w\|로 희소성을, L2는 w²로 큰 Weight 억제를 유도합니다.

### 상세 설명

L1은 \|w\|로 희소성을, L2는 w²로 큰 Weight 억제를 유도합니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 핵심 수식

$$
L_{L1}=L_{\mathrm{data}}+\lambda\sum_i|w_i|,\qquad L_{L2}=L_{\mathrm{data}}+\lambda\sum_iw_i^2
$$

### 면접 핵심

> L1은 \|w\|로 희소성을, L2는 w²로 큰 Weight 억제를 유도합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 034. Dropout이란?

### 답변

학습 중 일부 뉴런을 확률적으로 비활성화하는 Regularization입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> 학습 중 일부 뉴런을 확률적으로 비활성화하는 Regularization입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 035. Batch Normalization이란?

### 답변

Mini-batch 통계로 활성값을 정규화하고 학습 가능한 scale/shift를
적용합니다.

### 상세 설명

Mini-batch 통계로 활성값을 정규화하고 학습 가능한 scale/shift를
적용합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Mini-batch 통계로 활성값을 정규화하고 학습 가능한 scale/shift를
> 적용합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 036. Class Imbalance란?

### 답변

클래스별 데이터 수가 크게 불균형한 상황으로 Accuracy만으로 평가하면
위험합니다.

### 상세 설명

클래스별 데이터 수가 크게 불균형한 상황으로 Accuracy만으로 평가하면
위험합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 클래스별 데이터 수가 크게 불균형한 상황으로 Accuracy만으로 평가하면
> 위험합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 037. ROC/AUC란?

### 답변

ROC는 Threshold별 TPR-FPR 관계, AUC는 ROC 아래 면적입니다.

### 상세 설명

ROC는 Threshold별 TPR-FPR 관계, AUC는 ROC 아래 면적입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> ROC는 Threshold별 TPR-FPR 관계, AUC는 ROC 아래 면적입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 038. PCA란?

### 답변

분산을 최대한 보존하는 직교 축을 찾아 저차원으로 선형 변환하는
방법입니다.

### 상세 설명

분산을 최대한 보존하는 직교 축을 찾아 저차원으로 선형 변환하는
방법입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 분산을 최대한 보존하는 직교 축을 찾아 저차원으로 선형 변환하는
> 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 039. Clustering이란?

### 답변

Label 없이 유사한 데이터를 그룹화하는 비지도학습입니다.

### 상세 설명

Label 없이 유사한 데이터를 그룹화하는 비지도학습입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> Label 없이 유사한 데이터를 그룹화하는 비지도학습입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 040. K-means는 어떻게 동작하나요?

### 답변

K개 중심 초기화→가까운 중심에 할당→중심 재계산을 수렴할 때까지
반복합니다.

### 상세 설명

K개 중심 초기화→가까운 중심에 할당→중심 재계산을 수렴할 때까지
반복합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> K개 중심 초기화→가까운 중심에 할당→중심 재계산을 수렴할 때까지
> 반복합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 041\~050 상세판

> 주제: 확률·통계 · 선형대수 · Attention

## 041. 확률이란?

### 답변

사건 발생 가능성을 0\~1 사이 값으로 표현합니다.

### 상세 설명

사건 발생 가능성을 0\~1 사이 값으로 표현합니다. 면접에서는 이 정의에
더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는
것이 중요합니다.

### 면접 핵심

> 사건 발생 가능성을 0\~1 사이 값으로 표현합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 042. 조건부 확률이란?

### 답변

B가 발생한 조건에서 A가 발생할 확률로 P(A\|B)=P(A∩B)/P(B)입니다.

### 상세 설명

B가 발생한 조건에서 A가 발생할 확률로 P(A\|B)=P(A∩B)/P(B)입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
P(A\mid B)=\frac{P(A\cap B)}{P(B)}
$$

### 면접 핵심

> B가 발생한 조건에서 A가 발생할 확률로 P(A\|B)=P(A∩B)/P(B)입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 043. Bayes 정리란?

### 답변

P(A\|B)=P(B\|A)P(A)/P(B)로 사전확률을 증거로 갱신합니다.

### 상세 설명

P(A\|B)=P(B\|A)P(A)/P(B)로 사전확률을 증거로 갱신합니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 핵심 수식

$$
P(A\mid B)=\frac{P(B\mid A)P(A)}{P(B)}
$$

### 면접 핵심

> P(A\|B)=P(B\|A)P(A)/P(B)로 사전확률을 증거로 갱신합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 044. 평균·분산·표준편차의 차이는?

### 답변

평균은 중심, 분산은 제곱 편차 평균, 표준편차는 분산의 제곱근입니다.

### 상세 설명

평균은 중심, 분산은 제곱 편차 평균, 표준편차는 분산의 제곱근입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
\mu=\frac{1}{N}\sum_i x_i,\qquad \sigma^2=\frac{1}{N}\sum_i(x_i-\mu)^2,\qquad \sigma=\sqrt{\sigma^2}
$$

### 면접 핵심

> 평균은 중심, 분산은 제곱 편차 평균, 표준편차는 분산의 제곱근입니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 045. 정규분포란?

### 답변

평균을 중심으로 좌우 대칭인 종 모양의 연속 확률분포입니다.

### 상세 설명

평균을 중심으로 좌우 대칭인 종 모양의 연속 확률분포입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 평균을 중심으로 좌우 대칭인 종 모양의 연속 확률분포입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 046. 공분산이란?

### 답변

두 변수가 함께 변화하는 방향과 정도를 나타냅니다.

### 상세 설명

두 변수가 함께 변화하는 방향과 정도를 나타냅니다. 면접에서는 이 정의에
더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는
것이 중요합니다.

### 핵심 수식

$$
\mathrm{Cov}(X,Y)=E[(X-\mu_X)(Y-\mu_Y)]
$$

### 면접 핵심

> 두 변수가 함께 변화하는 방향과 정도를 나타냅니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 047. 상관계수와 공분산의 차이는?

### 답변

상관계수는 공분산을 표준편차로 정규화한 -1\~1 범위의 값입니다.

### 상세 설명

상관계수는 공분산을 표준편차로 정규화한 -1\~1 범위의 값입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
\rho_{X,Y}=\frac{\mathrm{Cov}(X,Y)}{\sigma_X\sigma_Y}
$$

### 면접 핵심

> 상관계수는 공분산을 표준편차로 정규화한 -1\~1 범위의 값입니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 048. Vector란?

### 답변

여러 숫자의 순서 있는 배열이며 AI의 데이터·Embedding 표현에 사용됩니다.

### 상세 설명

여러 숫자의 순서 있는 배열이며 AI의 데이터·Embedding 표현에 사용됩니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 여러 숫자의 순서 있는 배열이며 AI의 데이터·Embedding 표현에
> 사용됩니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 049. Dot Product란?

### 답변

대응 원소를 곱해 더하는 연산 A·B=ΣA_iB_i입니다.

### 상세 설명

대응 원소를 곱해 더하는 연산 A·B=ΣA_iB_i입니다. 면접에서는 이 정의에
더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는
것이 중요합니다.

### 핵심 수식

$$
A\cdot B=\sum_i A_iB_i
$$

### 면접 핵심

> 대응 원소를 곱해 더하는 연산 A·B=ΣA_iB_i입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 050. Cosine Similarity와 Dot Product의 관계는?

### 답변

cos=(A·B)/(\|\|A\|\|\|\|B\|\|)이며 L2 정규화 Vector에서는 값이 같습니다.

### 상세 설명

cos=(A·B)/(\|\|A\|\|\|\|B\|\|)이며 L2 정규화 Vector에서는 값이 같습니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
\cos(A,B)=\frac{A\cdot B}{\|A\|\|B\|}
$$

### 면접 핵심

> cos=(A·B)/(\|\|A\|\|\|\|B\|\|)이며 L2 정규화 Vector에서는 값이
> 같습니다.

### 꼬리질문

**Q. Cosine Similarity와 Dot Product가 언제 같아지나요?**

두 Vector를 L2 normalization하여 norm이 모두 1이면 cosine(A,B)=A·B가
됩니다.

------------------------------------------------------------------------

# GomsBook AI Interview 051\~060 상세판

> 주제: 확률·통계 · 선형대수 · Attention

## 051. Matrix란?

### 답변

숫자를 행과 열로 배열한 객체이며 딥러닝 선형변환의 기본 표현입니다.

### 상세 설명

숫자를 행과 열로 배열한 객체이며 딥러닝 선형변환의 기본 표현입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 숫자를 행과 열로 배열한 객체이며 딥러닝 선형변환의 기본 표현입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 052. 행렬 곱셈이란?

### 답변

(m×n)(n×p)→(m×p)이며 각 원소는 행과 열의 Dot Product입니다.

### 상세 설명

(m×n)(n×p)→(m×p)이며 각 원소는 행과 열의 Dot Product입니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> (m×n)(n×p)→(m×p)이며 각 원소는 행과 열의 Dot Product입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 053. Transpose란?

### 답변

행과 열을 바꾸는 연산으로 Attention의 QK\^T에 사용됩니다.

### 상세 설명

행과 열을 바꾸는 연산으로 Attention의 QK\^T에 사용됩니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 행과 열을 바꾸는 연산으로 Attention의 QK\^T에 사용됩니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 054. Rank란?

### 답변

행렬의 선형독립 행/열 최대 개수로 독립 정보 차원을 나타냅니다.

### 상세 설명

행렬의 선형독립 행/열 최대 개수로 독립 정보 차원을 나타냅니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 행렬의 선형독립 행/열 최대 개수로 독립 정보 차원을 나타냅니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 055. Linear Independence란?

### 답변

어떤 벡터도 다른 벡터들의 선형결합으로 표현되지 않는 상태입니다.

### 상세 설명

어떤 벡터도 다른 벡터들의 선형결합으로 표현되지 않는 상태입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 어떤 벡터도 다른 벡터들의 선형결합으로 표현되지 않는 상태입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 056. Eigenvalue/Eigenvector란?

### 답변

Av=λv에서 v는 고유벡터, λ는 고유값입니다.

### 상세 설명

Av=λv에서 v는 고유벡터, λ는 고유값입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
Av=\lambda v
$$

### 면접 핵심

> Av=λv에서 v는 고유벡터, λ는 고유값입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 057. Eigen과 PCA의 관계는?

### 답변

공분산 행렬의 큰 고유값에 대응하는 고유벡터가 주요 주성분이 됩니다.

### 상세 설명

공분산 행렬의 큰 고유값에 대응하는 고유벡터가 주요 주성분이 됩니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 공분산 행렬의 큰 고유값에 대응하는 고유벡터가 주요 주성분이 됩니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 058. SVD란?

### 답변

A=UΣV\^T로 행렬을 분해하는 방법입니다.

### 상세 설명

A=UΣV\^T로 행렬을 분해하는 방법입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
A=U\Sigma V^T
$$

### 면접 핵심

> A=UΣV\^T로 행렬을 분해하는 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 059. Attention에서 QK\^T의 의미는?

### 답변

각 Query와 Key 사이 Dot Product를 한 번에 계산한 Attention Score
행렬입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> 각 Query와 Key 사이 Dot Product를 한 번에 계산한 Attention Score
> 행렬입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 060. Attention 수식을 설명해보세요.

### 답변

softmax(QK\^T/√d_k)V: 관련성 계산→Scaling→Softmax→Value 가중합입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 핵심 수식

$$
\mathrm{Attention}(Q,K,V)=\mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V
$$

흐름: `Q,K,V 생성 → QK^T score → Scaling → Softmax → V 가중합`

### 면접 핵심

> softmax(QK\^T/√d_k)V: 관련성 계산→Scaling→Softmax→Value 가중합입니다.

### 꼬리질문

**Q. 왜 √d_k로 나누나요?**

d_k가 커질수록 Q·K의 Dot Product 분산이 커질 수 있고 Softmax가 포화되면
Gradient가 작아질 수 있습니다. √d_k scaling은 score scale을
안정화합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 061\~070 상세판

> 주제: RAG · Retrieval · Agentic RAG

## 061. Embedding이란?

### 답변

비정형 데이터를 의미적 Vector로 표현하는 방법입니다.

### 상세 설명

Embedding의 핵심은 의미적으로 비슷한 입력이 Vector 공간에서도 가깝도록
표현하는 것입니다. RAG에서는 Document와 Query를 같은 Embedding 공간으로
보내 Cosine Similarity나 Dot Product로 후보를 찾습니다.

### 면접 핵심

> 비정형 데이터를 의미적 Vector로 표현하는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 062. Vector Database란?

### 답변

Embedding과 Metadata를 저장하고 유사 Vector를 효율적으로 검색하는
시스템입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Embedding과 Metadata를 저장하고 유사 Vector를 효율적으로 검색하는
> 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 063. Vector Store와 Vector DB는 같은가요?

### 답변

혼용되지만 DB는 영속성·인덱싱·필터링·확장성까지 강조합니다.

### 상세 설명

혼용되지만 DB는 영속성·인덱싱·필터링·확장성까지 강조합니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 혼용되지만 DB는 영속성·인덱싱·필터링·확장성까지 강조합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 064. Chunking이 필요한 이유는?

### 답변

긴 문서를 적절한 단위로 나눠 필요한 부분을 더 정확히 검색하기
위해서입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 긴 문서를 적절한 단위로 나눠 필요한 부분을 더 정확히 검색하기
> 위해서입니다.

### GomsBook 프로젝트 연결

EPUB의 XHTML을 Chunking할 때 단순 문자 길이만 보지 않고
제목·문단·Section 구조를 활용할 수 있습니다. Child Chunk 검색 후
Parent/인접 문맥을 확장하는 방식은 EPUB 구조를 활용한 Retrieval 연구와
직접 연결됩니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 065. Chunk Overlap은 왜 사용하나요?

### 답변

Chunk 경계의 문맥 손실을 줄이기 위해 일부 내용을 중복합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Chunk 경계의 문맥 손실을 줄이기 위해 일부 내용을 중복합니다.

### GomsBook 프로젝트 연결

EPUB의 XHTML을 Chunking할 때 단순 문자 길이만 보지 않고
제목·문단·Section 구조를 활용할 수 있습니다. Child Chunk 검색 후
Parent/인접 문맥을 확장하는 방식은 EPUB 구조를 활용한 Retrieval 연구와
직접 연결됩니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 066. Top-K란?

### 답변

유사도 상위 K개의 검색 결과를 선택하는 것입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 유사도 상위 K개의 검색 결과를 선택하는 것입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 067. RAG 전체 과정은?

### 답변

문서→Chunk→Embedding→Store, Query→Embedding→Retrieval→Top-K
Context→LLM입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

흐름: `Document → Chunk → Embedding → Vector Store` /
`Query → Embedding → Retrieval → Context → LLM`

### 면접 핵심

> 문서→Chunk→Embedding→Store, Query→Embedding→Retrieval→Top-K
> Context→LLM입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 068. Semantic Search와 Keyword Search 차이는?

### 답변

Keyword는 문자열 일치, Semantic은 Embedding 의미 유사성에 강합니다.

### 상세 설명

Keyword는 문자열 일치, Semantic은 Embedding 의미 유사성에 강합니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Keyword는 문자열 일치, Semantic은 Embedding 의미 유사성에 강합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 069. Hybrid Search란?

### 답변

Keyword와 Vector Search를 결합해 결과를 융합하는 방식입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Keyword와 Vector Search를 결합해 결과를 융합하는 방식입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 070. Re-ranking이란?

### 답변

1차 검색 후보를 더 정교한 기준으로 재평가해 순서를 조정합니다.

### 상세 설명

1차 검색 후보를 더 정교한 기준으로 재평가해 순서를 조정합니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 1차 검색 후보를 더 정교한 기준으로 재평가해 순서를 조정합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 071\~080 상세판

> 주제: RAG · Retrieval · Agentic RAG

## 071. RAG 성능은 어떻게 평가하나요?

### 답변

Retrieval과 Generation을 분리해 Recall@K/MRR와 Correctness/Faithfulness
등을 봅니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Retrieval과 Generation을 분리해 Recall@K/MRR와
> Correctness/Faithfulness 등을 봅니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 072. Recall@K란?

### 답변

관련 문서 중 Top-K 안에서 찾아낸 비율입니다.

### 상세 설명

관련 문서 중 Top-K 안에서 찾아낸 비율입니다. 면접에서는 이 정의에 더해
적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
\mathrm{Recall@K}=\frac{\mathrm{Relevant\ items\ in\ Top\text{-}K}}{\mathrm{All\ relevant\ items}}
$$

### 면접 핵심

> 관련 문서 중 Top-K 안에서 찾아낸 비율입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 073. Precision@K란?

### 답변

Top-K 중 실제 관련 문서의 비율입니다.

### 상세 설명

Top-K 중 실제 관련 문서의 비율입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
\mathrm{Precision@K}=\frac{\mathrm{Relevant\ items\ in\ Top\text{-}K}}{K}
$$

### 면접 핵심

> Top-K 중 실제 관련 문서의 비율입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 074. MRR이란?

### 답변

첫 관련 결과의 1/rank를 여러 Query에 대해 평균한 값입니다.

### 상세 설명

첫 관련 결과의 1/rank를 여러 Query에 대해 평균한 값입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 핵심 수식

$$
\mathrm{MRR}=\frac{1}{|Q|}\sum_{q\in Q}\frac{1}{\mathrm{rank}_q}
$$

### 면접 핵심

> 첫 관련 결과의 1/rank를 여러 Query에 대해 평균한 값입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 075. Context Window란?

### 답변

한 요청에서 모델이 처리할 수 있는 Token 문맥 범위입니다.

### 상세 설명

한 요청에서 모델이 처리할 수 있는 Token 문맥 범위입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 한 요청에서 모델이 처리할 수 있는 Token 문맥 범위입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 076. Metadata Filtering이란?

### 답변

Vector 유사도와 함께 chapter/file/language 등의 조건으로 검색 범위를
제한합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Vector 유사도와 함께 chapter/file/language 등의 조건으로 검색 범위를
> 제한합니다.

### GomsBook 프로젝트 연결

`projectId`, `sourcePath`, chapter 같은 Metadata를 이용해 다른 EPUB
프로젝트의 Chunk가 섞이지 않도록 검색 범위를 제한하는 데 적용할 수
있습니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 077. Query Expansion이란?

### 답변

원 Query에 관련 표현을 추가해 검색 Recall을 높이는 방법입니다.

### 상세 설명

원 Query에 관련 표현을 추가해 검색 Recall을 높이는 방법입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 원 Query에 관련 표현을 추가해 검색 Recall을 높이는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 078. Multi-Query Retrieval이란?

### 답변

질문을 여러 Query로 변환해 검색한 결과를 통합합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 질문을 여러 Query로 변환해 검색한 결과를 통합합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 079. RAG와 Fine-tuning 중 무엇을 선택하나요?

### 답변

외부·최신 지식은 RAG, 모델 행동·작업 적응은 Fine-tuning을 우선
고려합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 외부·최신 지식은 RAG, 모델 행동·작업 적응은 Fine-tuning을 우선
> 고려합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 080. Agentic RAG란?

### 답변

Agent가 검색 여부·Query 수정·재검색 등을 동적으로 결정하는 RAG입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Agent가 검색 여부·Query 수정·재검색 등을 동적으로 결정하는 RAG입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

# GomsBook AI Interview 081\~090 상세판

> 주제: AI Agent · MCP · Prompt · 평가

## 081. AI Agent란?

### 답변

목표에 따라 Tool과 행동을 선택·실행하는 시스템입니다.

### 상세 설명

Agent는 단순 Chat Completion보다 한 단계 넓은 실행 시스템입니다. LLM이
판단을 담당하고 Runtime이 Tool 실행·상태·반복·오류·권한을 관리합니다.
따라서 Agent의 품질은 답변 품질뿐 아니라 행동의 정확성과 안전성까지
포함합니다.

### 면접 핵심

> 목표에 따라 Tool과 행동을 선택·실행하는 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 082. Tool Calling이란?

### 답변

LLM이 필요한 외부 기능과 Arguments를 구조화해 호출하도록 하는
방식입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> LLM이 필요한 외부 기능과 Arguments를 구조화해 호출하도록 하는
> 방식입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 083. Function Calling과 Tool Calling의 차이는?

### 답변

Function Calling은 함수 호출에 초점, Tool Calling은 검색·API·DB 등 더
넓은 외부 능력을 포함할 수 있습니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Function Calling은 함수 호출에 초점, Tool Calling은 검색·API·DB 등 더
> 넓은 외부 능력을 포함할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 084. Agent Loop란?

### 답변

판단→Action→Tool→Observation→재판단을 반복하는 구조입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 판단→Action→Tool→Observation→재판단을 반복하는 구조입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 085. Planning이란?

### 답변

복잡한 목표를 단계로 나누고 실행 순서를 결정하는 과정입니다.

### 상세 설명

복잡한 목표를 단계로 나누고 실행 순서를 결정하는 과정입니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 복잡한 목표를 단계로 나누고 실행 순서를 결정하는 과정입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 086. Agent Memory란?

### 답변

이전 상호작용이나 작업 상태를 이후 판단에 활용하는 메커니즘입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 이전 상호작용이나 작업 상태를 이후 판단에 활용하는 메커니즘입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 087. MCP란?

### 답변

AI 애플리케이션이 Tools/Resources/Prompts와 표준 방식으로 연결되는 Model
Context Protocol입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> AI 애플리케이션이 Tools/Resources/Prompts와 표준 방식으로 연결되는
> Model Context Protocol입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 088. Agent와 MCP 차이는?

### 답변

Agent는 판단·실행 로직, MCP는 연결 프로토콜입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Agent는 판단·실행 로직, MCP는 연결 프로토콜입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 089. RAG와 Agent 차이는?

### 답변

RAG는 지식 검색 패턴, Agent는 행동과 Tool을 결정·실행하는 시스템입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> RAG는 지식 검색 패턴, Agent는 행동과 Tool을 결정·실행하는
> 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 090. Agentic RAG란?

### 답변

Agent가 Retrieval 과정의 의사결정을 수행하는 구조입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Agent가 Retrieval 과정의 의사결정을 수행하는 구조입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

# GomsBook AI Interview 091\~100 상세판

> 주제: AI Agent · MCP · Prompt · 평가

## 091. Prompt Engineering이란?

### 답변

지시·Context·예시·출력 형식을 설계해 모델 행동을 유도합니다.

### 상세 설명

지시·Context·예시·출력 형식을 설계해 모델 행동을 유도합니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 지시·Context·예시·출력 형식을 설계해 모델 행동을 유도합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 092. Zero-shot과 Few-shot 차이는?

### 답변

Zero-shot은 예제 없음, Few-shot은 소수 예제를 제공합니다.

### 상세 설명

Zero-shot은 예제 없음, Few-shot은 소수 예제를 제공합니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> Zero-shot은 예제 없음, Few-shot은 소수 예제를 제공합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 093. Chain-of-Thought란?

### 답변

복잡한 문제에서 중간 추론 단계를 활용하는 접근입니다.

### 상세 설명

복잡한 문제에서 중간 추론 단계를 활용하는 접근입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 복잡한 문제에서 중간 추론 단계를 활용하는 접근입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 094. Structured Output이란?

### 답변

JSON/Schema 등 미리 정의된 구조로 모델 출력을 제한·검증하는 방식입니다.

### 상세 설명

JSON/Schema 등 미리 정의된 구조로 모델 출력을 제한·검증하는 방식입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> JSON/Schema 등 미리 정의된 구조로 모델 출력을 제한·검증하는
> 방식입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 095. Hallucination이란?

### 답변

근거 없거나 사실과 다른 내용을 그럴듯하게 생성하는 현상입니다.

### 상세 설명

근거 없거나 사실과 다른 내용을 그럴듯하게 생성하는 현상입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 근거 없거나 사실과 다른 내용을 그럴듯하게 생성하는 현상입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 096. Guardrail이란?

### 답변

입력·출력·Tool 실행에 검증·권한·제약을 적용하는 제어 장치입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 입력·출력·Tool 실행에 검증·권한·제약을 적용하는 제어 장치입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 097. Human-in-the-loop란?

### 답변

중요 단계에서 사람이 검토·승인하도록 하는 설계입니다.

### 상세 설명

중요 단계에서 사람이 검토·승인하도록 하는 설계입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 중요 단계에서 사람이 검토·승인하도록 하는 설계입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 098. LLM은 어떻게 평가하나요?

### 답변

Correctness, Relevance, Faithfulness, Retrieval, Latency, Cost, Tool
Success 등을 목적에 맞게 평가합니다.

### 상세 설명

Correctness, Relevance, Faithfulness, Retrieval, Latency, Cost, Tool
Success 등을 목적에 맞게 평가합니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> Correctness, Relevance, Faithfulness, Retrieval, Latency, Cost, Tool
> Success 등을 목적에 맞게 평가합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 099. AI 시스템 재현성이 어려운 이유는?

### 답변

Sampling, 모델 버전, Prompt, Context, 검색 결과 등이 변할 수 있기
때문입니다.

### 상세 설명

Sampling, 모델 버전, Prompt, Context, 검색 결과 등이 변할 수 있기
때문입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Sampling, 모델 버전, Prompt, Context, 검색 결과 등이 변할 수 있기
> 때문입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 100. 프로젝트를 AI 연구로 발전시킨다면?

### 답변

EPUB 저작·접근성 자동화에서 RAG/Agent를 적용하고 검색 및 작업 성능을
정량 평가합니다.

### 상세 설명

EPUB 저작·접근성 자동화에서 RAG/Agent를 적용하고 검색 및 작업 성능을
정량 평가합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> EPUB 저작·접근성 자동화에서 RAG/Agent를 적용하고 검색 및 작업 성능을
> 정량 평가합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 101\~110 상세판

> 주제: 심화 면접 · EPUB RAG · Agent 평가

## 101. RAG 검색이 틀렸는데 LLM이 정답이면 좋은 RAG인가요?

### 답변

아닙니다. 최종 답이 맞아도 올바른 근거를 찾지 못했다면 Retrieval
관점에서는 실패입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 아닙니다. 최종 답이 맞아도 올바른 근거를 찾지 못했다면 Retrieval
> 관점에서는 실패입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 102. 모든 질문에 RAG를 쓰면 더 좋나요?

### 답변

아닙니다. 불필요한 검색은 비용·Latency·Noise를 늘릴 수 있습니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 아닙니다. 불필요한 검색은 비용·Latency·Noise를 늘릴 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 103. Agent가 꼭 필요한가요?

### 답변

아닙니다. 결정론적 절차는 Workflow가 적합하고 동적 Tool 선택이 필요한
부분에 Agent가 유용합니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 아닙니다. 결정론적 절차는 Workflow가 적합하고 동적 Tool 선택이 필요한
> 부분에 Agent가 유용합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 104. EPUB Validator가 있는데 왜 LLM이 필요한가요?

### 답변

규칙 기반 검사는 구조적 오류에, LLM/Vision은 ALT 품질 같은 의미적 판단에
강해 Hybrid 구성이 가능합니다.

### 상세 설명

규칙 기반 검사는 구조적 오류에, LLM/Vision은 ALT 품질 같은 의미적 판단에
강해 Hybrid 구성이 가능합니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 규칙 기반 검사는 구조적 오류에, LLM/Vision은 ALT 품질 같은 의미적
> 판단에 강해 Hybrid 구성이 가능합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 105. LLM이 Validator보다 더 좋은가요?

### 답변

항상 그렇지 않습니다. 명확한 규칙은 결정론적 Validator가 더 정확하고
재현 가능합니다.

### 상세 설명

항상 그렇지 않습니다. 명확한 규칙은 결정론적 Validator가 더 정확하고
재현 가능합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 항상 그렇지 않습니다. 명확한 규칙은 결정론적 Validator가 더 정확하고
> 재현 가능합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 106. LLM이 잘못된 ALT를 생성하면?

### 답변

Prompt 제약, Validation, Rule Check, Human Review 후 적용해야 합니다.

### 상세 설명

Prompt 제약, Validation, Rule Check, Human Review 후 적용해야 합니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Prompt 제약, Validation, Rule Check, Human Review 후 적용해야 합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 107. Embedding 모델을 바꾸면 검색 결과도 달라지나요?

### 답변

네. 표현 공간이 달라 동일 Query라도 Retrieval 성능이 달라질 수 있습니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 네. 표현 공간이 달라 동일 Query라도 Retrieval 성능이 달라질 수
> 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 108. Chunk Size는 어떻게 결정하나요?

### 답변

여러 크기와 Overlap을 동일 Dataset에서 Recall@K/MRR/답변 품질로 비교해
결정합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 여러 크기와 Overlap을 동일 Dataset에서 Recall@K/MRR/답변 품질로 비교해
> 결정합니다.

### GomsBook 프로젝트 연결

EPUB의 XHTML을 Chunking할 때 단순 문자 길이만 보지 않고
제목·문단·Section 구조를 활용할 수 있습니다. Child Chunk 검색 후
Parent/인접 문맥을 확장하는 방식은 EPUB 구조를 활용한 Retrieval 연구와
직접 연결됩니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 109. 왜 EPUB RAG인가요?

### 답변

EPUB은 XHTML 구조·Metadata·이미지·접근성 속성을 Retrieval에 활용할 수
있는 구조화 문서입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> EPUB은 XHTML 구조·Metadata·이미지·접근성 속성을 Retrieval에 활용할 수
> 있는 구조화 문서입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 110. 석사 연구의 핵심 가설은?

### 답변

EPUB 구조·Metadata를 활용한 RAG/Agent가 일반 Text Retrieval보다
저작·접근성 작업 성능을 높이는지 검증합니다.

### 상세 설명

EPUB 구조·Metadata를 활용한 RAG/Agent가 일반 Text Retrieval보다
저작·접근성 작업 성능을 높이는지 검증합니다. 면접에서는 이 정의에 더해
적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> EPUB 구조·Metadata를 활용한 RAG/Agent가 일반 Text Retrieval보다
> 저작·접근성 작업 성능을 높이는지 검증합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 111\~120 상세판

> 주제: 심화 면접 · EPUB RAG · Agent 평가

## 111. 왜 MCP를 직접 구현했나요?

### 답변

프로토콜 내부 구조와 Agent-Tool 분리를 이해하기 위한 엔지니어링 기반
작업입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 프로토콜 내부 구조와 Agent-Tool 분리를 이해하기 위한 엔지니어링 기반
> 작업입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 112. MCP 없이도 Tool Calling이 가능한데 왜 필요한가요?

### 답변

가능하지만 MCP는 외부 AI 시스템과 Tool/Context를 표준 인터페이스로
연결합니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 가능하지만 MCP는 외부 AI 시스템과 Tool/Context를 표준 인터페이스로
> 연결합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 113. RAG에 Vector DB가 반드시 필요한가요?

### 답변

아닙니다. 소규모에서는 InMemory VectorStore와 Exact Search도 충분합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 아닙니다. 소규모에서는 InMemory VectorStore와 Exact Search도
> 충분합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 114. Vector가 수백만 개라면 모두 비교하나요?

### 답변

대규모에서는 HNSW/IVF 같은 ANN을 사용해 속도와 Recall의 Trade-off를
관리합니다.

### 상세 설명

대규모에서는 HNSW/IVF 같은 ANN을 사용해 속도와 Recall의 Trade-off를
관리합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 대규모에서는 HNSW/IVF 같은 ANN을 사용해 속도와 Recall의 Trade-off를
> 관리합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 115. Agent Tool 선택 정확도는 어떻게 평가하나요?

### 답변

기대 Tool이 정의된 Dataset으로 Tool Selection Accuracy를 측정하고
Argument/Task Success도 평가합니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 기대 Tool이 정의된 Dataset으로 Tool Selection Accuracy를 측정하고
> Argument/Task Success도 평가합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 116. Tool은 맞지만 Argument가 틀리면 성공인가요?

### 답변

아닙니다. Selection, Argument, Validation, Execution, End-to-End
Success를 분리해 봐야 합니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 아닙니다. Selection, Argument, Validation, Execution, End-to-End
> Success를 분리해 봐야 합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 117. LLM-as-a-Judge는 신뢰할 수 있나요?

### 답변

유용하지만 Bias가 있어 Automatic Metrics와 Human Evaluation을 함께
사용하는 것이 좋습니다.

### 상세 설명

유용하지만 Bias가 있어 Automatic Metrics와 Human Evaluation을 함께
사용하는 것이 좋습니다. 면접에서는 이 정의에 더해 적용 목적, 장점과
한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 유용하지만 Bias가 있어 Automatic Metrics와 Human Evaluation을 함께
> 사용하는 것이 좋습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 118. Baseline이 왜 중요하나요?

### 답변

제안 방법의 효과를 객관적으로 판단할 비교 기준이기 때문입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 제안 방법의 효과를 객관적으로 판단할 비교 기준이기 때문입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 119. EPUB RAG Baseline은?

### 답변

Keyword→Basic Vector RAG→Vector+Metadata→Structure-aware RAG처럼
단계적으로 비교할 수 있습니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Keyword→Basic Vector RAG→Vector+Metadata→Structure-aware RAG처럼
> 단계적으로 비교할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 120. GomsBook에서 연구 가치가 높은 부분은?

### 답변

EPUB 구조·접근성 정보를 활용한 Structure-aware RAG와 Agent 결합을 정량
검증하는 부분입니다.

### 상세 설명

EPUB 구조·접근성 정보를 활용한 Structure-aware RAG와 Agent 결합을 정량
검증하는 부분입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계,
인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> EPUB 구조·접근성 정보를 활용한 Structure-aware RAG와 Agent 결합을 정량
> 검증하는 부분입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 121\~130 상세판

> 주제: 연구방법론 · LLM 학습·튜닝

## 121. 왜 AI 대학원에 진학하려고 하나요?

### 답변

구현 경험을 이론적 이해와 정량적 연구로 발전시키고 AI 기반을 체계적으로
학습하기 위해서입니다.

### 상세 설명

구현 경험을 이론적 이해와 정량적 연구로 발전시키고 AI 기반을 체계적으로
학습하기 위해서입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계,
인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 구현 경험을 이론적 이해와 정량적 연구로 발전시키고 AI 기반을
> 체계적으로 학습하기 위해서입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 122. 왜 지금 AI 분야로 전환하려고 하나요?

### 답변

기존 소프트웨어 경험에 AI를 결합해 지능형 애플리케이션 설계 역량으로
확장하기 위해서입니다.

### 상세 설명

기존 소프트웨어 경험에 AI를 결합해 지능형 애플리케이션 설계 역량으로
확장하기 위해서입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계,
인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 기존 소프트웨어 경험에 AI를 결합해 지능형 애플리케이션 설계 역량으로
> 확장하기 위해서입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 123. AI 연구 경험이 부족한데 가능한가요?

### 답변

구현 경험을 Evaluation Dataset, Baseline, Recall@K/MRR/F1 같은 정량
평가로 연결해 보완할 수 있습니다.

### 상세 설명

구현 경험을 Evaluation Dataset, Baseline, Recall@K/MRR/F1 같은 정량
평가로 연결해 보완할 수 있습니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 구현 경험을 Evaluation Dataset, Baseline, Recall@K/MRR/F1 같은 정량
> 평가로 연결해 보완할 수 있습니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 124. 연구 주제는 어떻게 선정하나요?

### 답변

기술 나열이 아니라 해결할 문제와 검증 가능한 Research Question에서
출발합니다.

### 상세 설명

기술 나열이 아니라 해결할 문제와 검증 가능한 Research Question에서
출발합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 기술 나열이 아니라 해결할 문제와 검증 가능한 Research Question에서
> 출발합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 125. 가설이란?

### 답변

연구를 통해 검증할 수 있도록 구체화한 예상 또는 주장입니다.

### 상세 설명

연구를 통해 검증할 수 있도록 구체화한 예상 또는 주장입니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 연구를 통해 검증할 수 있도록 구체화한 예상 또는 주장입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 126. 실험 설계에서 중요한 것은?

### 답변

비교 변수 외 조건을 통제해 성능 차이의 원인을 해석할 수 있게 하는
것입니다.

### 상세 설명

비교 변수 외 조건을 통제해 성능 차이의 원인을 해석할 수 있게 하는
것입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 비교 변수 외 조건을 통제해 성능 차이의 원인을 해석할 수 있게 하는
> 것입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 127. Ablation Study란?

### 답변

구성 요소를 제거·변경하며 각 요소의 성능 기여도를 분석하는 실험입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 구성 요소를 제거·변경하며 각 요소의 성능 기여도를 분석하는 실험입니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 128. 재현성은 왜 중요하나요?

### 답변

다른 연구자가 유사 조건에서 결과를 반복·검증할 수 있어야 신뢰성이
높아집니다.

### 상세 설명

다른 연구자가 유사 조건에서 결과를 반복·검증할 수 있어야 신뢰성이
높아집니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 다른 연구자가 유사 조건에서 결과를 반복·검증할 수 있어야 신뢰성이
> 높아집니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 129. 예상과 다른 결과면 연구 실패인가요?

### 답변

아닙니다. 가설이 지지되지 않은 결과도 연구 결과이며 원인 분석이
중요합니다.

### 상세 설명

아닙니다. 가설이 지지되지 않은 결과도 연구 결과이며 원인 분석이
중요합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 아닙니다. 가설이 지지되지 않은 결과도 연구 결과이며 원인 분석이
> 중요합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 130. 석사 논문의 기여는?

### 답변

EPUB Structure-aware Retrieval과 Agent 기반 접근성 자동화를 설계하고
Baseline 대비 개선을 정량 검증하는 것입니다.

### 상세 설명

EPUB Structure-aware Retrieval과 Agent 기반 접근성 자동화를 설계하고
Baseline 대비 개선을 정량 검증하는 것입니다. 면접에서는 이 정의에 더해
적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> EPUB Structure-aware Retrieval과 Agent 기반 접근성 자동화를 설계하고
> Baseline 대비 개선을 정량 검증하는 것입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 131\~140 상세판

> 주제: 연구방법론 · LLM 학습·튜닝

## 131. LLM Pre-training이란?

### 답변

대규모 데이터에서 일반 언어 패턴을 학습하는 단계이며 Decoder-only 모델은
주로 다음 Token을 예측합니다.

### 상세 설명

대규모 데이터에서 일반 언어 패턴을 학습하는 단계이며 Decoder-only 모델은
주로 다음 Token을 예측합니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 대규모 데이터에서 일반 언어 패턴을 학습하는 단계이며 Decoder-only
> 모델은 주로 다음 Token을 예측합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 132. SFT란?

### 답변

입력-목표 출력 쌍으로 사전학습 모델을 지시 수행에 맞게 추가 학습하는
Supervised Fine-Tuning입니다.

### 상세 설명

입력-목표 출력 쌍으로 사전학습 모델을 지시 수행에 맞게 추가 학습하는
Supervised Fine-Tuning입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 입력-목표 출력 쌍으로 사전학습 모델을 지시 수행에 맞게 추가 학습하는
> Supervised Fine-Tuning입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 133. RLHF란?

### 답변

인간 선호 피드백을 이용해 모델 응답을 정렬하는 Reinforcement Learning
접근입니다.

### 상세 설명

인간 선호 피드백을 이용해 모델 응답을 정렬하는 Reinforcement Learning
접근입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 인간 선호 피드백을 이용해 모델 응답을 정렬하는 Reinforcement Learning
> 접근입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 134. DPO란?

### 답변

Chosen/Rejected preference pair를 이용해 별도 Reward Model·전통적 RL
없이 직접 최적화하는 방법입니다.

### 상세 설명

Chosen/Rejected preference pair를 이용해 별도 Reward Model·전통적 RL
없이 직접 최적화하는 방법입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Chosen/Rejected preference pair를 이용해 별도 Reward Model·전통적 RL
> 없이 직접 최적화하는 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 135. LoRA란?

### 답변

저랭크 행렬로 Weight 변화량을 학습해 학습 파라미터 수를 줄이는 PEFT
기법입니다.

### 상세 설명

저랭크 행렬로 Weight 변화량을 학습해 학습 파라미터 수를 줄이는 PEFT
기법입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 저랭크 행렬로 Weight 변화량을 학습해 학습 파라미터 수를 줄이는 PEFT
> 기법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 136. PEFT란?

### 답변

전체가 아닌 일부/추가 파라미터만 학습해 모델을 효율적으로 적응시키는
방법군입니다.

### 상세 설명

전체가 아닌 일부/추가 파라미터만 학습해 모델을 효율적으로 적응시키는
방법군입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 전체가 아닌 일부/추가 파라미터만 학습해 모델을 효율적으로 적응시키는
> 방법군입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 137. Quantization이란?

### 답변

Weight/Activation의 수치 정밀도를 낮춰 메모리·연산 비용을 줄이는
방법입니다.

### 상세 설명

Weight/Activation의 수치 정밀도를 낮춰 메모리·연산 비용을 줄이는
방법입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Weight/Activation의 수치 정밀도를 낮춰 메모리·연산 비용을 줄이는
> 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 138. LoRA와 Quantization 차이는?

### 답변

LoRA는 효율적 학습, Quantization은 수치 표현 경량화입니다. QLoRA는 둘을
결합합니다.

### 상세 설명

LoRA는 효율적 학습, Quantization은 수치 표현 경량화입니다. QLoRA는 둘을
결합합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> LoRA는 효율적 학습, Quantization은 수치 표현 경량화입니다. QLoRA는
> 둘을 결합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 139. KV Cache란?

### 답변

이전 Token의 Key/Value를 저장·재사용해 Autoregressive 생성의 중복 계산을
줄입니다.

### 상세 설명

이전 Token의 Key/Value를 저장·재사용해 Autoregressive 생성의 중복 계산을
줄입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 이전 Token의 Key/Value를 저장·재사용해 Autoregressive 생성의 중복
> 계산을 줄입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 140. Local LLM과 Cloud LLM 선택 기준은?

### 답변

데이터 통제, 오프라인, 인프라, 성능, 비용, 운영 편의를 기준으로
선택합니다.

### 상세 설명

데이터 통제, 오프라인, 인프라, 성능, 비용, 운영 편의를 기준으로
선택합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 데이터 통제, 오프라인, 인프라, 성능, 비용, 운영 편의를 기준으로
> 선택합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 141\~150 상세판

> 주제: Tokenization · Decoding · 수학 계산

## 141. Tokenization이란?

### 답변

문자열을 Token으로 분해하고 Token ID로 변환하는 과정입니다.

### 상세 설명

문자열을 Token으로 분해하고 Token ID로 변환하는 과정입니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 문자열을 Token으로 분해하고 Token ID로 변환하는 과정입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 142. BPE란?

### 답변

빈도가 높은 문자/심볼 조합을 반복 병합하는 Subword Tokenization
방식입니다.

### 상세 설명

빈도가 높은 문자/심볼 조합을 반복 병합하는 Subword Tokenization
방식입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 빈도가 높은 문자/심볼 조합을 반복 병합하는 Subword Tokenization
> 방식입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 143. Vocabulary란?

### 답변

Tokenizer가 사용할 수 있는 Token 집합입니다.

### 상세 설명

Tokenizer가 사용할 수 있는 Token 집합입니다. 면접에서는 이 정의에 더해
적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> Tokenizer가 사용할 수 있는 Token 집합입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 144. Logit이란?

### 답변

Softmax 전 각 Token/Class 후보에 대한 정규화되지 않은 점수입니다.

### 상세 설명

Softmax 전 각 Token/Class 후보에 대한 정규화되지 않은 점수입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Softmax 전 각 Token/Class 후보에 대한 정규화되지 않은 점수입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 145. Greedy Decoding이란?

### 답변

각 단계에서 가장 높은 점수의 Token 하나를 선택합니다.

### 상세 설명

각 단계에서 가장 높은 점수의 Token 하나를 선택합니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 각 단계에서 가장 높은 점수의 Token 하나를 선택합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 146. Top-K Sampling이란?

### 답변

상위 K개 Token만 후보로 남겨 Sampling합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 상위 K개 Token만 후보로 남겨 Sampling합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 147. Top-P Sampling이란?

### 답변

누적 확률이 P에 도달하는 후보 집합에서 Sampling하는 Nucleus
Sampling입니다.

### 상세 설명

누적 확률이 P에 도달하는 후보 집합에서 Sampling하는 Nucleus
Sampling입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 누적 확률이 P에 도달하는 후보 집합에서 Sampling하는 Nucleus
> Sampling입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 148. Temperature는 어떻게 적용되나요?

### 답변

Softmax 전에 Logit을 T로 나누며 낮을수록 분포가 날카롭고 높을수록
평평해집니다.

### 상세 설명

Softmax 전에 Logit을 T로 나누며 낮을수록 분포가 날카롭고 높을수록
평평해집니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Softmax 전에 Logit을 T로 나누며 낮을수록 분포가 날카롭고 높을수록
> 평평해집니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 149. Perplexity란?

### 답변

언어모델의 다음 Token 예측 불확실성 지표로 PPL=e\^L로 표현할 수
있습니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\mathrm{PPL}=\exp\left(-\frac{1}{N}\sum_{t=1}^{N}\log P(x_t\mid x_{<t})\right)
$$

### 면접 핵심

> 언어모델의 다음 Token 예측 불확실성 지표로 PPL=e\^L로 표현할 수
> 있습니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 150. MoE란?

### 답변

Router가 여러 Expert 중 Token별 일부만 선택해 처리하는 Mixture of
Experts 구조입니다.

### 상세 설명

Router가 여러 Expert 중 Token별 일부만 선택해 처리하는 Mixture of
Experts 구조입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계,
인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Router가 여러 Expert 중 Token별 일부만 선택해 처리하는 Mixture of
> Experts 구조입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 151\~160 상세판

> 주제: Tokenization · Decoding · 수학 계산

## 151. 미분과 Gradient란?

### 답변

미분은 변화율, Gradient는 다변수 함수의 편미분을 모은 벡터입니다.

### 상세 설명

미분은 변화율, Gradient는 다변수 함수의 편미분을 모은 벡터입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
\nabla_\theta L=\left[\frac{\partial L}{\partial\theta_1},\ldots,\frac{\partial L}{\partial\theta_n}\right]^T
$$

### 면접 핵심

> 미분은 변화율, Gradient는 다변수 함수의 편미분을 모은 벡터입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 152. Chain Rule이란?

### 답변

합성함수 미분 법칙이며 Backpropagation의 핵심입니다.

### 상세 설명

합성함수 미분 법칙이며 Backpropagation의 핵심입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 핵심 수식

$$
\frac{dy}{dx}=\frac{dy}{du}\frac{du}{dx}
$$

### 면접 핵심

> 합성함수 미분 법칙이며 Backpropagation의 핵심입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 153. 편미분이란?

### 답변

다른 변수는 상수로 두고 특정 변수 하나에 대해 미분합니다.

### 상세 설명

다른 변수는 상수로 두고 특정 변수 하나에 대해 미분합니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> 다른 변수는 상수로 두고 특정 변수 하나에 대해 미분합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 154. Gradient Descent 계산 예시는?

### 답변

L=w²,w=4,η=0.1이면 gradient=8, w_new=3.2입니다.

### 상세 설명

Gradient는 Loss가 가장 빠르게 증가하는 방향이므로 그 반대 방향으로
이동합니다. 학습률 η가 너무 크면 최소점을 지나치거나 발산하고, 너무
작으면 수렴이 느립니다.

### 핵심 수식

$$
L(w)=w^2,\quad \frac{dL}{dw}=2w,\quad w_{\mathrm{new}}=w-\eta\frac{dL}{dw}
$$

### 면접 핵심

> L=w²,w=4,η=0.1이면 gradient=8, w_new=3.2입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 155. Softmax 계산 예시는?

### 답변

z=\[2,1,0\]이면 약 \[0.665,0.245,0.090\]입니다.

### 상세 설명

Softmax는 Logit의 상대적 크기를 확률로 바꿉니다. 수치 안정성을 위해 실제
구현에서는 모든 Logit에서 max(z)를 뺀 뒤 exp를 계산하는 방식이
일반적입니다.

### 핵심 수식

$$
p_i=\frac{e^{z_i}}{\sum_j e^{z_j}}
$$

### 면접 핵심

> z=\[2,1,0\]이면 약 \[0.665,0.245,0.090\]입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 156. Cross Entropy 계산 예시는?

### 답변

정답 Class 확률이 0.665이면 L=-log(0.665)≈0.408입니다.

### 상세 설명

정답 클래스에 높은 확률을 줄수록 Loss가 작아지고, 정답 확률이 0에
가까워질수록 큰 패널티를 받습니다. Softmax와 함께 쓰면 Logit에 대한
Gradient가 p-y로 단순화되어 분류와 LLM next-token training에 자연스럽게
연결됩니다.

### 핵심 수식

$$
\mathcal{L}=-\log p_{\mathrm{correct}}
$$

### 면접 핵심

> 정답 Class 확률이 0.665이면 L=-log(0.665)≈0.408입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 157. Cosine Similarity 계산 예시는?

### 답변

A=(1,2),B=(2,1)이면 dot=4, norm은 각각 √5, cosine=0.8입니다.

### 상세 설명

A=(1,2),B=(2,1)이면 dot=4, norm은 각각 √5, cosine=0.8입니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 핵심 수식

$$
\cos(A,B)=\frac{A\cdot B}{\|A\|\|B\|}
$$

### 면접 핵심

> A=(1,2),B=(2,1)이면 dot=4, norm은 각각 √5, cosine=0.8입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 158. 행렬 곱셈 차원은?

### 답변

3×4와 4×5의 곱은 3×5입니다.

### 상세 설명

3×4와 4×5의 곱은 3×5입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과
한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 3×4와 4×5의 곱은 3×5입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 159. Attention 행렬 크기는?

### 답변

Q=10×64,K\^T=64×10이면 QK\^T=10×10입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> Q=10×64,K\^T=64×10이면 QK\^T=10×10입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 160. Scaled Dot-Product Attention 계산 예시는?

### 답변

동일 score의 두 Value에 weight 0.5씩이면 V1=\[2,0\],V2=\[0,4\]의 출력은
\[1,2\]입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> 동일 score의 두 Value에 weight 0.5씩이면 V1=\[2,0\],V2=\[0,4\]의
> 출력은 \[1,2\]입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 161\~170 상세판

> 주제: 미분 · 확률 계산 · Regression · Attention

## 161. Sigmoid 미분은?

### 답변

σ'(x)=σ(x)(1-σ(x)); x=0에서는 0.25입니다.

### 상세 설명

σ'(x)=σ(x)(1-σ(x)); x=0에서는 0.25입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
\sigma'(x)=\sigma(x)(1-\sigma(x))
$$

### 면접 핵심

> σ'(x)=σ(x)(1-σ(x)); x=0에서는 0.25입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 162. ReLU 미분은?

### 답변

x\<0에서 0, x\>0에서 1이며 x=0은 미분 불가능해 구현상 subgradient를
정합니다.

### 상세 설명

x\<0에서 0, x\>0에서 1이며 x=0은 미분 불가능해 구현상 subgradient를
정합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> x\<0에서 0, x\>0에서 1이며 x=0은 미분 불가능해 구현상 subgradient를
> 정합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 163. Softmax+Cross Entropy Gradient는?

### 답변

Logit에 대한 Gradient가 p-y로 단순화됩니다.

### 상세 설명

정답 클래스에 높은 확률을 줄수록 Loss가 작아지고, 정답 확률이 0에
가까워질수록 큰 패널티를 받습니다. Softmax와 함께 쓰면 Logit에 대한
Gradient가 p-y로 단순화되어 분류와 LLM next-token training에 자연스럽게
연결됩니다.

### 핵심 수식

$$
\frac{\partial \mathcal{L}}{\partial z}=p-y
$$

### 면접 핵심

> Logit에 대한 Gradient가 p-y로 단순화됩니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 164. MSE 계산 예시는?

### 답변

y=\[3,5,2\],ŷ=\[2,4,4\]이면 MSE=(1+1+4)/3=2입니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\mathrm{MSE}=\frac{1}{N}\sum_i(y_i-\hat y_i)^2
$$

### 면접 핵심

> y=\[3,5,2\],ŷ=\[2,4,4\]이면 MSE=(1+1+4)/3=2입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 165. 평균과 분산 계산 예시는?

### 답변

\[2,4,6,8\]의 평균=5, 모집단 분산=5, 표준편차=√5입니다.

### 상세 설명

\[2,4,6,8\]의 평균=5, 모집단 분산=5, 표준편차=√5입니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> \[2,4,6,8\]의 평균=5, 모집단 분산=5, 표준편차=√5입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 166. Z-score 계산 예시는?

### 답변

μ=50,σ=10,x=70이면 z=2입니다.

### 상세 설명

μ=50,σ=10,x=70이면 z=2입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
z=\frac{x-\mu}{\sigma}
$$

### 면접 핵심

> μ=50,σ=10,x=70이면 z=2입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 167. Bayes 계산 예시는?

### 답변

P(E)=0.1,P(D\|E)=0.9,P(D\|N)=0.05이면 P(E\|D)=0.09/0.135≈0.667입니다.

### 상세 설명

P(E)=0.1,P(D\|E)=0.9,P(D\|N)=0.05이면 P(E\|D)=0.09/0.135≈0.667입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
P(E\mid D)=\frac{P(D\mid E)P(E)}{P(D)}
$$

### 면접 핵심

> P(E)=0.1,P(D\|E)=0.9,P(D\|N)=0.05이면 P(E\|D)=0.09/0.135≈0.667입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 168. 조건부확률 계산 예시는?

### 답변

ALT 오류 20개 중 장식 이미지 5개면 P(Decorative\|AltError)=0.25입니다.

### 상세 설명

ALT 오류 20개 중 장식 이미지 5개면 P(Decorative\|AltError)=0.25입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> ALT 오류 20개 중 장식 이미지 5개면 P(Decorative\|AltError)=0.25입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 169. Precision/Recall/F1 계산 예시는?

### 답변

TP=80,FP=20,FN=40이면 P=0.8,R≈0.667,F1≈0.727입니다.

### 상세 설명

TP=80,FP=20,FN=40이면 P=0.8,R≈0.667,F1≈0.727입니다. 면접에서는 이 정의에
더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는
것이 중요합니다.

### 핵심 수식

$$
F_1=2\frac{\mathrm{Precision}\cdot\mathrm{Recall}}{\mathrm{Precision}+\mathrm{Recall}}
$$

### 면접 핵심

> TP=80,FP=20,FN=40이면 P=0.8,R≈0.667,F1≈0.727입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 170. PCA를 간단히 계산하면?

### 답변

(1,1),(2,2),(3,3),(4,4)는 (1,1) 방향에 분산이 집중되어 1차원으로 축소
가능합니다.

### 상세 설명

(1,1),(2,2),(3,3),(4,4)는 (1,1) 방향에 분산이 집중되어 1차원으로 축소
가능합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> (1,1),(2,2),(3,3),(4,4)는 (1,1) 방향에 분산이 집중되어 1차원으로 축소
> 가능합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 171\~180 상세판

> 주제: 미분 · 확률 계산 · Regression · Attention

## 171. Linear Regression 기본 식은?

### 답변

ŷ=wx+b, 다변수에서는 ŷ=XW+b입니다.

### 상세 설명

ŷ=wx+b, 다변수에서는 ŷ=XW+b입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
\hat y=wx+b,\qquad \hat y=XW+b
$$

### 면접 핵심

> ŷ=wx+b, 다변수에서는 ŷ=XW+b입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 172. Linear Regression Gradient 예시는?

### 답변

L=(y-wx)², x=2,y=10,w=3이면 gradient=-16, η=0.1이면 w_new=4.6입니다.

### 상세 설명

L=(y-wx)², x=2,y=10,w=3이면 gradient=-16, η=0.1이면 w_new=4.6입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
L=(y-wx)^2,\qquad \frac{\partial L}{\partial w}=-2x(y-wx)
$$

### 면접 핵심

> L=(y-wx)², x=2,y=10,w=3이면 gradient=-16, η=0.1이면 w_new=4.6입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 173. Logistic Regression은 무엇이 다른가요?

### 답변

선형결합을 Sigmoid에 통과시켜 0\~1 확률을 출력합니다.

### 상세 설명

선형결합을 Sigmoid에 통과시켜 0\~1 확률을 출력합니다. 면접에서는 이
정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 핵심 수식

$$
P(y=1\mid x)=\sigma(w^Tx+b)
$$

### 면접 핵심

> 선형결합을 Sigmoid에 통과시켜 0\~1 확률을 출력합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 174. Binary Cross Entropy 계산은?

### 답변

y=1,p=0.8이면 BCE=-log(0.8)≈0.223입니다.

### 상세 설명

정답 클래스에 높은 확률을 줄수록 Loss가 작아지고, 정답 확률이 0에
가까워질수록 큰 패널티를 받습니다. Softmax와 함께 쓰면 Logit에 대한
Gradient가 p-y로 단순화되어 분류와 LLM next-token training에 자연스럽게
연결됩니다.

### 핵심 수식

$$
\mathrm{BCE}=-[y\log p+(1-y)\log(1-p)]
$$

### 면접 핵심

> y=1,p=0.8이면 BCE=-log(0.8)≈0.223입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 175. L2 Regularization 계산은?

### 답변

Loss=2,w=\[1,2,3\],λ=0.1이면 penalty=1.4,total=3.4입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 핵심 수식

$$
L=L_{\mathrm{data}}+\lambda\sum_iw_i^2
$$

### 면접 핵심

> Loss=2,w=\[1,2,3\],λ=0.1이면 penalty=1.4,total=3.4입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 176. L1 Regularization 계산은?

### 답변

같은 조건이면 penalty=0.6,total=2.6입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 핵심 수식

$$
L=L_{\mathrm{data}}+\lambda\sum_i|w_i|
$$

### 면접 핵심

> 같은 조건이면 penalty=0.6,total=2.6입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 177. Attention Mask란?

### 답변

특정 Token 위치를 Attention에서 참조하지 못하게 제한합니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> 특정 Token 위치를 Attention에서 참조하지 못하게 제한합니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 178. Causal Mask란?

### 답변

현재 Token이 미래 Token을 보지 못하게 하는 Decoder용 Mask입니다.

### 상세 설명

현재 Token이 미래 Token을 보지 못하게 하는 Decoder용 Mask입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 현재 Token이 미래 Token을 보지 못하게 하는 Decoder용 Mask입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 179. Multi-Head Attention 차원 계산은?

### 답변

d_model=512,h=8이면 head당 64차원이고 concatenate 후 512차원입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> d_model=512,h=8이면 head당 64차원이고 concatenate 후 512차원입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 180. QKV Projection Parameter 수는?

### 답변

d_model=512이면 QKV는 3×512²=786,432개, Output까지
4×512²=1,048,576개입니다.

### 상세 설명

d_model=512이면 QKV는 3×512²=786,432개, Output까지
4×512²=1,048,576개입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과
한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
N_{\mathrm{QKV}}=3d_{\mathrm{model}}^2,\qquad N_{\mathrm{attn,total}}\approx4d_{\mathrm{model}}^2
$$

### 면접 핵심

> d_model=512이면 QKV는 3×512²=786,432개, Output까지
> 4×512²=1,048,576개입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 181\~190 상세판

> 주제: Transformer Block · 생성 평가

## 181. Transformer Block의 주요 구성은?

### 답변

Multi-Head Attention, Residual Connection, LayerNorm, FFN이 핵심입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> Multi-Head Attention, Residual Connection, LayerNorm, FFN이
> 핵심입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 182. FFN이란?

### 답변

각 Token 위치에 독립적으로 적용되는 MLP로 보통 d_model→d_ff→d_model
구조입니다.

### 상세 설명

각 Token 위치에 독립적으로 적용되는 MLP로 보통 d_model→d_ff→d_model
구조입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
N_{\mathrm{FFN}}\approx 2d_{\mathrm{model}}d_{\mathrm{ff}}
$$

### 면접 핵심

> 각 Token 위치에 독립적으로 적용되는 MLP로 보통 d_model→d_ff→d_model
> 구조입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 183. FFN 차원을 크게 잡는 이유는?

### 답변

Token별 표현을 더 높은 차원에서 비선형 변환해 표현력을 높이기
위해서입니다.

### 상세 설명

Token별 표현을 더 높은 차원에서 비선형 변환해 표현력을 높이기
위해서입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Token별 표현을 더 높은 차원에서 비선형 변환해 표현력을 높이기
> 위해서입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 184. Pre-LN과 Post-LN 차이는?

### 답변

Post-LN은 y=LN(x+F(x)), Pre-LN은 y=x+F(LN(x))이며 Pre-LN이 깊은 모델
학습 안정성에 유리한 경우가 많습니다.

### 상세 설명

Post-LN은 y=LN(x+F(x)), Pre-LN은 y=x+F(LN(x))이며 Pre-LN이 깊은 모델
학습 안정성에 유리한 경우가 많습니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
\mathrm{PostLN}:\ y=\mathrm{LN}(x+F(x)),\qquad \mathrm{PreLN}:\ y=x+F(\mathrm{LN}(x))
$$

### 면접 핵심

> Post-LN은 y=LN(x+F(x)), Pre-LN은 y=x+F(LN(x))이며 Pre-LN이 깊은 모델
> 학습 안정성에 유리한 경우가 많습니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 185. Residual Connection이란?

### 답변

입력 x를 변환 결과 F(x)에 더해 y=x+F(x)로 만드는 skip connection입니다.

### 상세 설명

입력 x를 변환 결과 F(x)에 더해 y=x+F(x)로 만드는 skip connection입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 핵심 수식

$$
y=x+F(x)
$$

### 면접 핵심

> 입력 x를 변환 결과 F(x)에 더해 y=x+F(x)로 만드는 skip
> connection입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 186. Transformer Encoder와 Decoder 차이는?

### 답변

Encoder는 입력 전체를 양방향으로 참조하고, autoregressive Decoder는
Causal Mask로 미래 Token을 보지 못합니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> Encoder는 입력 전체를 양방향으로 참조하고, autoregressive Decoder는
> Causal Mask로 미래 Token을 보지 못합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 187. Cross-Attention이란?

### 답변

Query는 한 시퀀스에서, Key/Value는 다른 시퀀스에서 가져와 두 표현을
연결하는 Attention입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 면접 핵심

> Query는 한 시퀀스에서, Key/Value는 다른 시퀀스에서 가져와 두 표현을
> 연결하는 Attention입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 188. Beam Search란?

### 답변

각 단계에서 상위 여러 후보 시퀀스를 유지하며 전체 점수가 높은 시퀀스를
탐색하는 Decoding 방법입니다.

### 상세 설명

각 단계에서 상위 여러 후보 시퀀스를 유지하며 전체 점수가 높은 시퀀스를
탐색하는 Decoding 방법입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 각 단계에서 상위 여러 후보 시퀀스를 유지하며 전체 점수가 높은 시퀀스를
> 탐색하는 Decoding 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 189. BLEU와 ROUGE 차이는?

### 답변

BLEU는 주로 n-gram precision, ROUGE는 주로 recall 관점의 중복을 강조하는
생성 평가 지표입니다.

### 상세 설명

BLEU는 주로 n-gram precision, ROUGE는 주로 recall 관점의 중복을 강조하는
생성 평가 지표입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계,
인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> BLEU는 주로 n-gram precision, ROUGE는 주로 recall 관점의 중복을
> 강조하는 생성 평가 지표입니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 190. 생성 평가의 한계는?

### 답변

문자열 중복만으로 의미·사실성·근거성을 충분히 평가하기 어려워
Semantic/Factual/RAG 지표가 필요합니다.

### 상세 설명

문자열 중복만으로 의미·사실성·근거성을 충분히 평가하기 어려워
Semantic/Factual/RAG 지표가 필요합니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> 문자열 중복만으로 의미·사실성·근거성을 충분히 평가하기 어려워
> Semantic/Factual/RAG 지표가 필요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 191\~200 상세판

> 주제: RAG Evaluation

## 191. Faithfulness란?

### 답변

생성 답변의 주장들이 제공된 Context에 의해 뒷받침되는 정도입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 생성 답변의 주장들이 제공된 Context에 의해 뒷받침되는 정도입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 192. Answer Relevancy란?

### 답변

생성 답변이 사용자의 질문 의도에 얼마나 관련되고 직접적인지를
평가합니다.

### 상세 설명

생성 답변이 사용자의 질문 의도에 얼마나 관련되고 직접적인지를
평가합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 생성 답변이 사용자의 질문 의도에 얼마나 관련되고 직접적인지를
> 평가합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 193. Context Precision이란?

### 답변

검색된 Context 중 실제로 유용한 정보가 얼마나 상위에 정확히 배치됐는지를
평가합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 검색된 Context 중 실제로 유용한 정보가 얼마나 상위에 정확히
> 배치됐는지를 평가합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 194. Context Recall이란?

### 답변

정답에 필요한 정보가 검색 Context에 얼마나 빠짐없이 포함됐는지를
평가합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 정답에 필요한 정보가 검색 Context에 얼마나 빠짐없이 포함됐는지를
> 평가합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 195. Golden Dataset이란?

### 답변

질문, 기대 답변, 관련 근거 등 평가 기준을 미리 정의한 검증용
데이터셋입니다.

### 상세 설명

질문, 기대 답변, 관련 근거 등 평가 기준을 미리 정의한 검증용
데이터셋입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 질문, 기대 답변, 관련 근거 등 평가 기준을 미리 정의한 검증용
> 데이터셋입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 196. NO_ANSWER 테스트가 필요한 이유는?

### 답변

지식베이스에 근거가 없을 때 모델이 답을 꾸며내지 않고 답변 불가를
판단하는 능력을 평가하기 위해서입니다.

### 상세 설명

지식베이스에 근거가 없을 때 모델이 답을 꾸며내지 않고 답변 불가를
판단하는 능력을 평가하기 위해서입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> 지식베이스에 근거가 없을 때 모델이 답을 꾸며내지 않고 답변 불가를
> 판단하는 능력을 평가하기 위해서입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 197. LLM-as-a-Judge란?

### 답변

LLM을 평가자로 사용해 정답성·관련성·근거성 같은 정성적 품질을 점수화하는
방법입니다.

### 상세 설명

LLM을 평가자로 사용해 정답성·관련성·근거성 같은 정성적 품질을 점수화하는
방법입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> LLM을 평가자로 사용해 정답성·관련성·근거성 같은 정성적 품질을
> 점수화하는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 198. LLM-as-a-Judge 신뢰성을 높이려면?

### 답변

평가 기준과 Prompt를 고정하고 반복 평가·다른 지표·Human Review와 함께
사용해야 합니다.

### 상세 설명

평가 기준과 Prompt를 고정하고 반복 평가·다른 지표·Human Review와 함께
사용해야 합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 평가 기준과 Prompt를 고정하고 반복 평가·다른 지표·Human Review와 함께
> 사용해야 합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 199. Retrieval Failure와 Generation Failure를 왜 분리하나요?

### 답변

검색 실패와 생성 실패는 원인과 개선 방법이 다르므로 단계별로 진단해야
합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 검색 실패와 생성 실패는 원인과 개선 방법이 다르므로 단계별로 진단해야
> 합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 200. GomsBook RAG 평가 시스템을 설명해보세요.

### 답변

Golden Dataset으로 실제 RAG를 실행하고 Retrieval Context와 Answer를
수집해 Faithfulness, Answer Relevancy, Context Precision/Recall,
No-Answer Detection 등을 평가하는 구조입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Golden Dataset으로 실제 RAG를 실행하고 Retrieval Context와 Answer를
> 수집해 Faithfulness, Answer Relevancy, Context Precision/Recall,
> No-Answer Detection 등을 평가하는 구조입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 평균 점수만 높으면 RAG가 좋은 것인가요?**

아닙니다. 평균은 특정 실패를 숨길 수 있습니다. Case별 Context
Precision/Recall, Faithfulness, NO_ANSWER 실패를 보고 Retrieval과
Generation 오류를 분리해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 201\~210 상세판

> 주제: Retrieval 심화

## 201. Retriever의 역할은?

### 답변

Query와 관련성이 높은 문서 또는 Chunk를 찾아 LLM에 제공할 후보 Context를
선택하는 것입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Query와 관련성이 높은 문서 또는 Chunk를 찾아 LLM에 제공할 후보
> Context를 선택하는 것입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 202. Embedding은 Retrieval에서 어떤 역할을 하나요?

### 답변

Query와 Document를 같은 Vector 공간에 표현해 의미 유사도 기반 검색을
가능하게 합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Query와 Document를 같은 Vector 공간에 표현해 의미 유사도 기반 검색을
> 가능하게 합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 203. Cosine Similarity란?

### 답변

두 Vector의 각도 기반 유사도로 (A·B)/(\|\|A\|\|\|\|B\|\|)이며 방향이
비슷할수록 값이 큽니다.

### 상세 설명

두 Vector의 각도 기반 유사도로 (A·B)/(\|\|A\|\|\|\|B\|\|)이며 방향이
비슷할수록 값이 큽니다. 면접에서는 이 정의에 더해 적용 목적, 장점과
한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 두 Vector의 각도 기반 유사도로 (A·B)/(\|\|A\|\|\|\|B\|\|)이며 방향이
> 비슷할수록 값이 큽니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 204. Top-K는 어떻게 정하나요?

### 답변

작으면 Recall이 떨어지고 크면 Noise와 비용이 늘어 동일 평가셋에서 성능을
비교해 정합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 작으면 Recall이 떨어지고 크면 Noise와 비용이 늘어 동일 평가셋에서
> 성능을 비교해 정합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 205. Similarity Threshold란?

### 답변

유사도 점수가 기준보다 낮은 검색 결과를 제외하는 cutoff이며
모델·데이터별로 튜닝해야 합니다.

### 상세 설명

유사도 점수가 기준보다 낮은 검색 결과를 제외하는 cutoff이며
모델·데이터별로 튜닝해야 합니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 유사도 점수가 기준보다 낮은 검색 결과를 제외하는 cutoff이며
> 모델·데이터별로 튜닝해야 합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 206. Reranking이란?

### 답변

빠른 1차 검색으로 후보를 얻은 뒤 더 정밀한 모델이나 기준으로 후보 순위를
다시 계산하는 단계입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 빠른 1차 검색으로 후보를 얻은 뒤 더 정밀한 모델이나 기준으로 후보
> 순위를 다시 계산하는 단계입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 207. Chunking 전략의 Trade-off는?

### 답변

작은 Chunk는 검색 정밀도가 높지만 문맥이 잘릴 수 있고 큰 Chunk는 문맥은
풍부하지만 검색 신호가 희석될 수 있습니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 작은 Chunk는 검색 정밀도가 높지만 문맥이 잘릴 수 있고 큰 Chunk는
> 문맥은 풍부하지만 검색 신호가 희석될 수 있습니다.

### GomsBook 프로젝트 연결

EPUB의 XHTML을 Chunking할 때 단순 문자 길이만 보지 않고
제목·문단·Section 구조를 활용할 수 있습니다. Child Chunk 검색 후
Parent/인접 문맥을 확장하는 방식은 EPUB 구조를 활용한 Retrieval 연구와
직접 연결됩니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 208. Parent-Child Context Expansion이란?

### 답변

작은 Child Chunk로 정밀 검색한 뒤 Parent나 인접 Chunk까지 확장해 LLM에
충분한 문맥을 제공하는 방법입니다.

### 상세 설명

작은 Child Chunk로 정밀 검색한 뒤 Parent나 인접 Chunk까지 확장해 LLM에
충분한 문맥을 제공하는 방법입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 작은 Child Chunk로 정밀 검색한 뒤 Parent나 인접 Chunk까지 확장해 LLM에
> 충분한 문맥을 제공하는 방법입니다.

### GomsBook 프로젝트 연결

EPUB의 XHTML을 Chunking할 때 단순 문자 길이만 보지 않고
제목·문단·Section 구조를 활용할 수 있습니다. Child Chunk 검색 후
Parent/인접 문맥을 확장하는 방식은 EPUB 구조를 활용한 Retrieval 연구와
직접 연결됩니다.

### 꼬리질문

**Q. 왜 Child Chunk만 LLM에 전달하지 않나요?**

Child는 검색 정밀도에는 유리하지만 문맥이 잘릴 수 있습니다. 작은 Child로
검색하고 Parent/Neighbor를 확장하면 Retrieval precision과 Generation
context를 절충할 수 있습니다.

------------------------------------------------------------------------

## 209. Hybrid Search란?

### 답변

Dense Semantic Retrieval과 Sparse Keyword/BM25 Retrieval을 결합하는 검색
방식입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Dense Semantic Retrieval과 Sparse Keyword/BM25 Retrieval을 결합하는
> 검색 방식입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 210. RAG 개선 실험은 어떻게 설계하나요?

### 답변

Baseline을 고정하고 한 요소씩 변경해 동일 Dataset과 Metrics로 비교하는
Ablation 방식이 적합합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Baseline을 고정하고 한 요소씩 변경해 동일 Dataset과 Metrics로 비교하는
> Ablation 방식이 적합합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

# GomsBook AI Interview 211\~220 상세판

> 주제: Retrieval 심화

## 211. Vector Database란?

### 답변

Embedding Vector와 Metadata를 저장하고 유사도 검색 및 ANN 인덱스를
제공하는 데이터 시스템입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Embedding Vector와 Metadata를 저장하고 유사도 검색 및 ANN 인덱스를
> 제공하는 데이터 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 212. ANN이란?

### 답변

Approximate Nearest Neighbor로 정확한 전수 비교 대신 근사 탐색을 사용해
대규모 Vector 검색 속도를 높입니다.

### 상세 설명

Approximate Nearest Neighbor로 정확한 전수 비교 대신 근사 탐색을 사용해
대규모 Vector 검색 속도를 높입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Approximate Nearest Neighbor로 정확한 전수 비교 대신 근사 탐색을
> 사용해 대규모 Vector 검색 속도를 높입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 213. HNSW란?

### 답변

Hierarchical Navigable Small World graph를 이용해 상위 계층에서 거칠게
탐색하고 하위 계층에서 정밀하게 이웃을 찾는 ANN 방법입니다.

### 상세 설명

Hierarchical Navigable Small World graph를 이용해 상위 계층에서 거칠게
탐색하고 하위 계층에서 정밀하게 이웃을 찾는 ANN 방법입니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> Hierarchical Navigable Small World graph를 이용해 상위 계층에서 거칠게
> 탐색하고 하위 계층에서 정밀하게 이웃을 찾는 ANN 방법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 214. Dense와 Sparse Retrieval 차이는?

### 답변

Dense는 Embedding 의미 유사성, Sparse는 단어 빈도와 정확한 용어 일치에
강합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Dense는 Embedding 의미 유사성, Sparse는 단어 빈도와 정확한 용어 일치에
> 강합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 215. Query Expansion이란?

### 답변

원 질문에 관련 용어·동의어·표현을 추가해 Retrieval Recall을 높이는
방법입니다.

### 상세 설명

원 질문에 관련 용어·동의어·표현을 추가해 Retrieval Recall을 높이는
방법입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 원 질문에 관련 용어·동의어·표현을 추가해 Retrieval Recall을 높이는
> 방법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 216. Multi-Query Retrieval이란?

### 답변

하나의 질문을 여러 관점의 Query로 생성해 각각 검색하고 결과를 통합하는
방식입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 하나의 질문을 여러 관점의 Query로 생성해 각각 검색하고 결과를 통합하는
> 방식입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 217. HyDE란?

### 답변

LLM이 가상의 답변/문서를 먼저 생성하고 이를 Embedding해 실제 문서를
검색하는 Retrieval 기법입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> LLM이 가상의 답변/문서를 먼저 생성하고 이를 Embedding해 실제 문서를
> 검색하는 Retrieval 기법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 218. Metadata Filtering이 중요한 이유는?

### 답변

projectId, sourcePath, chapter 같은 조건으로 검색 범위를 제한해 다른
프로젝트나 불필요한 문서가 섞이는 것을 막습니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> projectId, sourcePath, chapter 같은 조건으로 검색 범위를 제한해 다른
> 프로젝트나 불필요한 문서가 섞이는 것을 막습니다.

### GomsBook 프로젝트 연결

`projectId`, `sourcePath`, chapter 같은 Metadata를 이용해 다른 EPUB
프로젝트의 Chunk가 섞이지 않도록 검색 범위를 제한하는 데 적용할 수
있습니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 219. RRF란?

### 답변

Reciprocal Rank Fusion으로 여러 검색기의 raw score 대신 순위를 이용해
결과를 결합하는 방법입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 핵심 수식

$$
\mathrm{RRF}(d)=\sum_r\frac{1}{k+\mathrm{rank}_r(d)}
$$

### 면접 핵심

> Reciprocal Rank Fusion으로 여러 검색기의 raw score 대신 순위를 이용해
> 결과를 결합하는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 220. Ablation Study란?

### 답변

시스템 구성 요소를 추가·제거하며 각 요소가 성능에 미치는 기여를 분리해
분석하는 실험입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 시스템 구성 요소를 추가·제거하며 각 요소가 성능에 미치는 기여를 분리해
> 분석하는 실험입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 221\~230 상세판

> 주제: RAG/LLM · Agent

## 221. RAG 실패의 주요 원인은?

### 답변

Retrieval miss/noise, 잘못된 Chunking, Context 부족, 생성 단계의 Context
무시나 Hallucination 등입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Retrieval miss/noise, 잘못된 Chunking, Context 부족, 생성 단계의
> Context 무시나 Hallucination 등입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 222. Hallucination이란?

### 답변

모델이 근거가 없거나 사실과 다른 내용을 그럴듯하게 생성하는 현상입니다.

### 상세 설명

모델이 근거가 없거나 사실과 다른 내용을 그럴듯하게 생성하는 현상입니다.
면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를
한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 모델이 근거가 없거나 사실과 다른 내용을 그럴듯하게 생성하는
> 현상입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 223. Retrieval Failure란?

### 답변

정답 근거가 문서에 존재하지만 Retriever가 관련 Chunk를 Top-K에 포함하지
못하는 실패입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 정답 근거가 문서에 존재하지만 Retriever가 관련 Chunk를 Top-K에
> 포함하지 못하는 실패입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 224. Lost in the Middle이란?

### 답변

긴 Context에서 중간 위치의 중요한 정보가 상대적으로 잘 활용되지 않는
현상을 말합니다.

### 상세 설명

긴 Context에서 중간 위치의 중요한 정보가 상대적으로 잘 활용되지 않는
현상을 말합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 긴 Context에서 중간 위치의 중요한 정보가 상대적으로 잘 활용되지 않는
> 현상을 말합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 225. Context Window가 크면 항상 좋은가요?

### 답변

아닙니다. 더 많은 문맥을 넣을 수 있지만 Noise, 비용, Latency와 정보 활용
저하가 생길 수 있습니다.

### 상세 설명

아닙니다. 더 많은 문맥을 넣을 수 있지만 Noise, 비용, Latency와 정보 활용
저하가 생길 수 있습니다. 면접에서는 이 정의에 더해 적용 목적, 장점과
한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 아닙니다. 더 많은 문맥을 넣을 수 있지만 Noise, 비용, Latency와 정보
> 활용 저하가 생길 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 226. Attention의 계산 복잡도는?

### 답변

표준 Self-Attention의 score 계산은 sequence length n에 대해 대략 O(n²d),
attention matrix memory는 O(n²)입니다.

### 상세 설명

Attention은 Query와 Key의 관련성을 점수화하고 Softmax로 정규화한 뒤
Value를 가중합합니다. √d_k로 나누는 이유는 차원이 커질수록 Dot Product
분산이 커져 Softmax가 과도하게 포화되는 것을 완화하기 위해서입니다.

### 핵심 수식

$$
\text{Attention score compute}\approx O(n^2d),\qquad \text{attention matrix memory}=O(n^2)
$$

### 면접 핵심

> 표준 Self-Attention의 score 계산은 sequence length n에 대해 대략
> O(n²d), attention matrix memory는 O(n²)입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 227. KV Cache란?

### 답변

Autoregressive 생성에서 이전 Token의 Key/Value projection을 저장해 매
단계의 중복 계산을 줄입니다.

### 상세 설명

Autoregressive 생성에서 이전 Token의 Key/Value projection을 저장해 매
단계의 중복 계산을 줄입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과
한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Autoregressive 생성에서 이전 Token의 Key/Value projection을 저장해 매
> 단계의 중복 계산을 줄입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 228. Quantization이란?

### 답변

FP32/FP16 등의 Weight·Activation을 INT8/INT4 등 낮은 정밀도로 표현해
메모리와 연산량을 줄이는 방법입니다.

### 상세 설명

FP32/FP16 등의 Weight·Activation을 INT8/INT4 등 낮은 정밀도로 표현해
메모리와 연산량을 줄이는 방법입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> FP32/FP16 등의 Weight·Activation을 INT8/INT4 등 낮은 정밀도로 표현해
> 메모리와 연산량을 줄이는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 229. LoRA란?

### 답변

기존 Weight를 고정하고 저랭크 행렬 BA로 표현한 변화량 ΔW를 학습하는
Parameter-Efficient Fine-Tuning 방법입니다.

### 상세 설명

기존 Weight를 고정하고 저랭크 행렬 BA로 표현한 변화량 ΔW를 학습하는
Parameter-Efficient Fine-Tuning 방법입니다. 면접에서는 이 정의에 더해
적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 핵심 수식

$$
W'=W+\Delta W,\qquad \Delta W=BA,\qquad \mathrm{rank}(BA)\le r
$$

### 면접 핵심

> 기존 Weight를 고정하고 저랭크 행렬 BA로 표현한 변화량 ΔW를 학습하는
> Parameter-Efficient Fine-Tuning 방법입니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 230. Fine-tuning과 RAG의 차이는?

### 답변

Fine-tuning은 모델 Parameter를 바꾸고 RAG는 외부 지식을 검색해 Context로
제공하므로 최신·변경 지식에는 RAG가 유리합니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Fine-tuning은 모델 Parameter를 바꾸고 RAG는 외부 지식을 검색해
> Context로 제공하므로 최신·변경 지식에는 RAG가 유리합니다.

### GomsBook 프로젝트 연결

GomsBook의 EPUB/XHTML RAG에서는 Query Embedding → project-scoped
Retrieval → Top-K/Reranking → Context Expansion → LLM Answer 흐름으로
연결할 수 있으며, Retrieval metric과 Generation metric을 따로 측정해야
합니다.

### 꼬리질문

**Q. Fine-tuning과 RAG를 함께 사용할 수 있나요?**

네. Fine-tuning으로 모델의 행동·형식·도메인 작업 패턴을 맞추고, RAG로
변경되는 외부 지식을 제공하는 식으로 역할을 분리할 수 있습니다.

------------------------------------------------------------------------

# GomsBook AI Interview 231\~240 상세판

> 주제: RAG/LLM · Agent

## 231. Prompt Engineering이란?

### 답변

Role, Instruction, Context, Constraints, Output Format, Examples 등을
설계해 모델 행동을 유도하는 방법입니다.

### 상세 설명

Role, Instruction, Context, Constraints, Output Format, Examples 등을
설계해 모델 행동을 유도하는 방법입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> Role, Instruction, Context, Constraints, Output Format, Examples 등을
> 설계해 모델 행동을 유도하는 방법입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 232. Zero-shot과 Few-shot의 차이는?

### 답변

Zero-shot은 예시 없이 지시만 제공하고 Few-shot은 소수의 입력-출력 예시를
Prompt에 포함합니다.

### 상세 설명

Zero-shot은 예시 없이 지시만 제공하고 Few-shot은 소수의 입력-출력 예시를
Prompt에 포함합니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계,
인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Zero-shot은 예시 없이 지시만 제공하고 Few-shot은 소수의 입력-출력
> 예시를 Prompt에 포함합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 233. In-Context Learning이란?

### 답변

Parameter를 업데이트하지 않고 Prompt의 예시와 Context에서 작업 패턴을
추론해 수행하는 능력입니다.

### 상세 설명

Parameter를 업데이트하지 않고 Prompt의 예시와 Context에서 작업 패턴을
추론해 수행하는 능력입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과
한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Parameter를 업데이트하지 않고 Prompt의 예시와 Context에서 작업 패턴을
> 추론해 수행하는 능력입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 234. Temperature와 Top-p 차이는?

### 답변

Temperature는 전체 확률분포의 날카로움을 조절하고 Top-p는 누적 확률
기준으로 Sampling 후보 집합을 제한합니다.

### 상세 설명

Temperature는 전체 확률분포의 날카로움을 조절하고 Top-p는 누적 확률
기준으로 Sampling 후보 집합을 제한합니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> Temperature는 전체 확률분포의 날카로움을 조절하고 Top-p는 누적 확률
> 기준으로 Sampling 후보 집합을 제한합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 235. Structured Output이 중요한 이유는?

### 답변

JSON/Schema 등 정해진 구조로 결과를 받아 프로그램이 안정적으로
파싱·검증·후속 처리할 수 있기 때문입니다.

### 상세 설명

JSON/Schema 등 정해진 구조로 결과를 받아 프로그램이 안정적으로
파싱·검증·후속 처리할 수 있기 때문입니다. 면접에서는 이 정의에 더해 적용
목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이
중요합니다.

### 면접 핵심

> JSON/Schema 등 정해진 구조로 결과를 받아 프로그램이 안정적으로
> 파싱·검증·후속 처리할 수 있기 때문입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 236. Function Calling이란?

### 답변

LLM이 실행할 함수와 Arguments를 선택하고 Application Runtime이 실제
함수를 실행해 결과를 모델에 돌려주는 방식입니다.

### 상세 설명

LLM이 실행할 함수와 Arguments를 선택하고 Application Runtime이 실제
함수를 실행해 결과를 모델에 돌려주는 방식입니다. 면접에서는 이 정의에
더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는
것이 중요합니다.

### 면접 핵심

> LLM이 실행할 함수와 Arguments를 선택하고 Application Runtime이 실제
> 함수를 실행해 결과를 모델에 돌려주는 방식입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 237. AI Agent란?

### 답변

LLM에 Tool, Memory/Context, 실행 제어를 결합해 목표에 따라 여러 단계의
행동을 수행하는 시스템입니다.

### 상세 설명

Agent는 단순 Chat Completion보다 한 단계 넓은 실행 시스템입니다. LLM이
판단을 담당하고 Runtime이 Tool 실행·상태·반복·오류·권한을 관리합니다.
따라서 Agent의 품질은 답변 품질뿐 아니라 행동의 정확성과 안전성까지
포함합니다.

### 면접 핵심

> LLM에 Tool, Memory/Context, 실행 제어를 결합해 목표에 따라 여러 단계의
> 행동을 수행하는 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 238. Tool Calling이란?

### 답변

Agent/LLM이 Tool Registry의 외부 기능 중 필요한 Tool과 Arguments를
선택해 Runtime에 실행을 요청하는 메커니즘입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Agent/LLM이 Tool Registry의 외부 기능 중 필요한 Tool과 Arguments를
> 선택해 Runtime에 실행을 요청하는 메커니즘입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 239. MCP란?

### 답변

Model Context Protocol로 AI 애플리케이션과 외부 Tool·Resource·Prompt
제공자를 표준 방식으로 연결하는 프로토콜입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Model Context Protocol로 AI 애플리케이션과 외부 Tool·Resource·Prompt
> 제공자를 표준 방식으로 연결하는 프로토콜입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. MCP와 Tool Calling은 같은 개념인가요?**

아닙니다. Tool Calling은 모델/Runtime이 Tool을 선택·호출하는
메커니즘이고, MCP는 AI 애플리케이션과 외부 Tool·Resource 제공자를
연결하는 표준 프로토콜입니다. 둘은 함께 사용할 수 있습니다.

------------------------------------------------------------------------

## 240. Agent + RAG 구조를 설명해보세요.

### 답변

Agent가 목표를 보고 Retrieval 필요 여부와 Tool을 결정하며 RAG는 Agent가
사용할 수 있는 지식 검색 능력 중 하나입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> Agent가 목표를 보고 Retrieval 필요 여부와 Tool을 결정하며 RAG는
> Agent가 사용할 수 있는 지식 검색 능력 중 하나입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

# GomsBook AI Interview 241\~250 상세판

> 주제: Agent Workflow · HITL · 평가

## 241. AI Agent의 Workflow는 어떻게 구성되나요?

### 답변

Goal→Reason/Plan→Action→Observation→Next Action→Final Answer의 반복 실행
구조로 볼 수 있습니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

흐름:
`Goal → Plan/Reason → Action → Observation → Next Action → Final Answer`

### 면접 핵심

> Goal→Reason/Plan→Action→Observation→Next Action→Final Answer의 반복
> 실행 구조로 볼 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 242. ReAct란 무엇인가요?

### 답변

Reasoning과 Acting을 결합해 판단→Action→Observation을 반복하며 문제를
해결하는 Agent 패턴입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Reasoning과 Acting을 결합해 판단→Action→Observation을 반복하며 문제를
> 해결하는 Agent 패턴입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 243. Agent에서 Planning이란 무엇인가요?

### 답변

복잡한 목표를 실행 가능한 Subtask로 분해하고 의존관계와 실행 순서를
결정하는 과정입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 복잡한 목표를 실행 가능한 Subtask로 분해하고 의존관계와 실행 순서를
> 결정하는 과정입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 244. AI Agent에서 Memory란 무엇인가요?

### 답변

이전 상호작용, Tool 결과, 작업 상태 등을 저장하고 이후 판단 시 필요한
정보를 다시 Context로 제공하는 메커니즘입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 이전 상호작용, Tool 결과, 작업 상태 등을 저장하고 이후 판단 시 필요한
> 정보를 다시 Context로 제공하는 메커니즘입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 245. Short-term Memory와 Long-term Memory 차이는?

### 답변

Short-term은 현재 Task/Session 상태, Long-term은 세션을 넘어 지속적으로
재사용할 정보나 지식을 저장합니다.

### 상세 설명

Short-term은 현재 Task/Session 상태, Long-term은 세션을 넘어 지속적으로
재사용할 정보나 지식을 저장합니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Short-term은 현재 Task/Session 상태, Long-term은 세션을 넘어
> 지속적으로 재사용할 정보나 지식을 저장합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 246. Human-in-the-Loop가 왜 필요한가요?

### 답변

위험하거나 중요한 변경을 AI가 즉시 실행하지 않고 사람이 검토·승인한
경우에만 적용해 신뢰성과 통제성을 높입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 위험하거나 중요한 변경을 AI가 즉시 실행하지 않고 사람이 검토·승인한
> 경우에만 적용해 신뢰성과 통제성을 높입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 모든 Tool 실행에 승인을 받아야 하나요?**

아닙니다. 읽기·조회처럼 부작용이 없는 작업은 자동 실행할 수 있고, 파일
수정·삭제·배포처럼 되돌리기 어렵거나 위험한 작업에 승인 정책을 집중하는
것이 효율적입니다.

------------------------------------------------------------------------

## 247. Agent가 무한루프에 빠지는 이유와 방지 방법은?

### 답변

같은 실패를 반복하거나 종료조건을 찾지 못해 발생하며 max iteration,
retry limit, timeout, 반복 감지와 명확한 종료 조건으로 제어합니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 같은 실패를 반복하거나 종료조건을 찾지 못해 발생하며 max iteration,
> retry limit, timeout, 반복 감지와 명확한 종료 조건으로 제어합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 248. Guardrails란 무엇인가요?

### 답변

AI가 허용된 범위에서 동작하도록 정책, 권한, 입력/출력 검증, Tool 제약,
승인 등을 적용하는 안전 제어 계층입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> AI가 허용된 범위에서 동작하도록 정책, 권한, 입력/출력 검증, Tool 제약,
> 승인 등을 적용하는 안전 제어 계층입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 249. AI Agent는 어떻게 평가해야 하나요?

### 답변

Task Success, Tool Selection/Execution, Step 수, Latency/Cost,
Generation 품질, Safety와 전체 Trajectory를 함께 평가해야 합니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Task Success, Tool Selection/Execution, Step 수, Latency/Cost,
> Generation 품질, Safety와 전체 Trajectory를 함께 평가해야 합니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 250. GomsBook AI Agent의 아키텍처를 설명해보세요.

### 답변

Chat UI→Agent Runtime→LLM/Tool Registry→EPUB/RAG/Accessibility Tools로
분리하고 중요한 변경은 HITL 승인 후 적용하며 MCP로 외부 Tool 연결을
표준화한 구조입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Chat UI→Agent Runtime→LLM/Tool Registry→EPUB/RAG/Accessibility Tools로
> 분리하고 중요한 변경은 HITL 승인 후 적용하며 MCP로 외부 Tool 연결을
> 표준화한 구조입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 251\~260 상세판

> 주제: Agentic Workflow · Multi-Agent · Security

## 251. AI Agent와 Workflow의 차이는 무엇인가요?

### 답변

Workflow는 실행 경로가 주로 코드로 사전 정의되고 Agent는 현재 상태와
Observation을 바탕으로 다음 행동을 동적으로 선택할 수 있습니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Workflow는 실행 경로가 주로 코드로 사전 정의되고 Agent는 현재 상태와
> Observation을 바탕으로 다음 행동을 동적으로 선택할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 252. Deterministic Workflow와 Agentic Workflow 차이는?

### 답변

Deterministic은 코드가 경로를 결정하고 Agentic은 모델이 일부 의사결정을
수행하며 실전에서는 deterministic control과 agentic decision을 결합할 수
있습니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Deterministic은 코드가 경로를 결정하고 Agentic은 모델이 일부
> 의사결정을 수행하며 실전에서는 deterministic control과 agentic
> decision을 결합할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 253. Agent Orchestration이란?

### 답변

Agent, LLM, Tool, RAG, Memory를 연결하고 상태, retry, timeout, approval,
종료조건 등 전체 실행 흐름을 관리하는 것입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Agent, LLM, Tool, RAG, Memory를 연결하고 상태, retry, timeout,
> approval, 종료조건 등 전체 실행 흐름을 관리하는 것입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 254. Multi-Agent System이란?

### 답변

역할이 다른 여러 Agent가 작업을 분담·협력하여 하나의 목표를 수행하는
시스템입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 역할이 다른 여러 Agent가 작업을 분담·협력하여 하나의 목표를 수행하는
> 시스템입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 255. Multi-Agent는 서로 어떻게 통신하나요?

### 답변

자연어 또는 구조화된 Message, Shared State, 중앙 Orchestrator 등을 통해
Task, Status, Result를 교환할 수 있습니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 자연어 또는 구조화된 Message, Shared State, 중앙 Orchestrator 등을
> 통해 Task, Status, Result를 교환할 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 256. Supervisor Agent란?

### 답변

여러 Worker Agent 중 어떤 Agent에게 어떤 작업을 맡길지 판단하고 결과를
취합하는 상위 조정 Agent입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 여러 Worker Agent 중 어떤 Agent에게 어떤 작업을 맡길지 판단하고 결과를
> 취합하는 상위 조정 Agent입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 257. AI Agent에서 Security가 중요한 이유는?

### 답변

Agent는 File, API, DB, Tool 등에 실제 Action을 수행할 수 있어 잘못된
판단이나 공격이 외부 시스템 변경으로 이어질 수 있기 때문입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> Agent는 File, API, DB, Tool 등에 실제 Action을 수행할 수 있어 잘못된
> 판단이나 공격이 외부 시스템 변경으로 이어질 수 있기 때문입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

## 258. Prompt Injection이란?

### 답변

악의적인 입력이 모델의 원래 지시나 정책보다 우선하도록 유도해 의도하지
않은 응답이나 행동을 일으키려는 공격입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 악의적인 입력이 모델의 원래 지시나 정책보다 우선하도록 유도해 의도하지
> 않은 응답이나 행동을 일으키려는 공격입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 259. RAG Prompt Injection이란?

### 답변

검색된 외부 문서 안의 악의적 지시가 LLM Context에 들어가 Agent 행동을
조작하는 Indirect Prompt Injection 위험입니다.

### 상세 설명

RAG에서는 Retrieval과 Generation을 분리해서 진단해야 합니다. 관련 근거가
Top-K에 없으면 Retrieval Failure이고, 근거가 있는데도 답변이 틀리거나
근거를 벗어나면 Generation Failure입니다. 따라서 단일 최종
정답률만으로는 개선 원인을 알기 어렵습니다.

### 면접 핵심

> 검색된 외부 문서 안의 악의적 지시가 LLM Context에 들어가 Agent 행동을
> 조작하는 Indirect Prompt Injection 위험입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. 성능이 낮다면 무엇부터 확인하나요?**

먼저 정답 근거가 Top-K에 들어오는지 확인합니다. 없다면 Retrieval
문제이므로 Query, Chunking, Embedding, Top-K, Threshold, Metadata를
점검하고, 근거가 있는데 답이 틀리면 Prompt와 Generation 단계로 범위를
좁힙니다.

------------------------------------------------------------------------

## 260. AI Agent 연구의 주요 한계는?

### 답변

신뢰성, 평가 어려움, 보안, 비용·Latency, 장기 작업의 오류 누적과 통제
가능성이 주요 한계입니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> 신뢰성, 평가 어려움, 보안, 비용·Latency, 장기 작업의 오류 누적과 통제
> 가능성이 주요 한계입니다.

### GomsBook 프로젝트 연결

GomsBook AI Agent에서는 LLM 판단과 Tool 실행을 분리하고, EPUB 변경
작업은 승인 상태를 거쳐 적용하도록 설계할 수 있습니다. 조회 Tool은
자동화하고 수정·삭제·생성처럼 부작용이 큰 Tool에는 HITL과 Validation을
적용하는 구조가 적합합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 261\~270 상세판

> 주제: AI 연구방법론

## 261. 연구 문제(Research Problem)는 어떻게 정의하나요?

### 답변

현재 방법의 한계와 해결·검증하려는 질문을 측정 가능한 형태로 정의해야
합니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 현재 방법의 한계와 해결·검증하려는 질문을 측정 가능한 형태로 정의해야
> 합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 262. Hypothesis란 무엇인가요?

### 답변

연구 문제에 대해 실험을 통해 지지 또는 기각 여부를 검증할 수 있도록 만든
구체적인 예상입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 연구 문제에 대해 실험을 통해 지지 또는 기각 여부를 검증할 수 있도록
> 만든 구체적인 예상입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 263. Baseline은 왜 필요한가요?

### 답변

제안 방법이 실제로 개선됐는지를 동일 조건에서 비교할 기준이 필요하기
때문입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 제안 방법이 실제로 개선됐는지를 동일 조건에서 비교할 기준이 필요하기
> 때문입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 264. Experimental Design이란?

### 답변

가설 검증을 위해 무엇을 변경하고 무엇을 통제하며 어떤 지표를 측정할지
설계하는 과정입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 가설 검증을 위해 무엇을 변경하고 무엇을 통제하며 어떤 지표를 측정할지
> 설계하는 과정입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 265. Independent Variable과 Dependent Variable은?

### 답변

독립변수는 연구자가 변경하는 요인이고 종속변수는 그 변화에 따라 측정되는
결과입니다.

### 상세 설명

독립변수는 연구자가 변경하는 요인이고 종속변수는 그 변화에 따라 측정되는
결과입니다. 면접에서는 이 정의에 더해 적용 목적, 장점과 한계, 인접
개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 독립변수는 연구자가 변경하는 요인이고 종속변수는 그 변화에 따라
> 측정되는 결과입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 266. Ablation Study는 연구에서 어떻게 사용하나요?

### 답변

구성 요소를 하나씩 추가·제거하거나 조합해 각 요소가 성능 향상에 실제로
기여하는 정도를 분석합니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 구성 요소를 하나씩 추가·제거하거나 조합해 각 요소가 성능 향상에 실제로
> 기여하는 정도를 분석합니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 좋은 실험인지 어떻게 판단하나요?**

비교할 Baseline이 있고, 독립변수 외 조건을 통제하며, 평가 Dataset과
Metric이 사전에 정의되고, 반복 가능한 설정과 Error Analysis가 있어야
합니다.

------------------------------------------------------------------------

## 267. Statistical Significance란?

### 답변

관찰된 차이가 우연한 표본 변동인지 실제 차이라고 볼 근거가 있는지를
통계적으로 판단하는 개념입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 관찰된 차이가 우연한 표본 변동인지 실제 차이라고 볼 근거가 있는지를
> 통계적으로 판단하는 개념입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 268. Reproducibility란?

### 답변

동일하거나 충분히 유사한 실험 조건에서 다른 연구자도 결과를 반복·검증할
수 있도록 하는 특성입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 동일하거나 충분히 유사한 실험 조건에서 다른 연구자도 결과를
> 반복·검증할 수 있도록 하는 특성입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 269. Error Analysis란?

### 답변

평균 점수만 보지 않고 실패 사례를 유형별로 분류해 원인과 개선 방향을
분석하는 과정입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 평균 점수만 보지 않고 실패 사례를 유형별로 분류해 원인과 개선 방향을
> 분석하는 과정입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 270. GomsBook AI Agent를 대학원 연구로 발전시킨다면?

### 답변

EPUB 구조를 활용한 Retrieval 전략과 HITL Agent의 신뢰성을 Baseline,
Golden Dataset, Ablation, Error Analysis로 정량 검증하는 연구로 발전시킬
수 있습니다.

### 상세 설명

Agentic 시스템의 핵심은 '모델의 판단'과 '실제 권한을 가진 실행'을
분리하는 것입니다. Tool allowlist, Argument/Schema 검증, 최대 반복 횟수,
Timeout, 승인, Audit Log 같은 제어는 LLM의 자연어 판단에만 의존하지 않고
Runtime에서 강제하는 것이 중요합니다.

### 면접 핵심

> EPUB 구조를 활용한 Retrieval 전략과 HITL Agent의 신뢰성을 Baseline,
> Golden Dataset, Ablation, Error Analysis로 정량 검증하는 연구로
> 발전시킬 수 있습니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. LLM에게 맡기지 말아야 할 부분은 무엇인가요?**

권한 검사, 허용 경로, Schema 검증, 최대 반복 횟수, Timeout, 승인 필요
여부 같은 안전 정책은 Application/Runtime이 결정론적으로 강제해야
합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 271\~280 상세판

> 주제: Generalization · Regularization

## 271. Overfitting이란 무엇인가요?

### 답변

Training Data에는 지나치게 잘 맞지만 새로운 데이터에 대한 일반화 성능이
떨어지는 현상입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> Training Data에는 지나치게 잘 맞지만 새로운 데이터에 대한 일반화
> 성능이 떨어지는 현상입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 272. Underfitting이란 무엇인가요?

### 답변

모델이 데이터의 기본 패턴조차 충분히 학습하지 못해 Training과 Validation
모두 성능이 낮은 상태입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> 모델이 데이터의 기본 패턴조차 충분히 학습하지 못해 Training과
> Validation 모두 성능이 낮은 상태입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 273. Bias-Variance Tradeoff란?

### 답변

모델의 단순화로 생기는 Bias와 데이터 변화에 대한 민감도인 Variance
사이의 균형을 통해 일반화 성능을 설명하는 개념입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> 모델의 단순화로 생기는 Bias와 데이터 변화에 대한 민감도인 Variance
> 사이의 균형을 통해 일반화 성능을 설명하는 개념입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 274. Train/Validation/Test Dataset의 역할은?

### 답변

Train은 Parameter 학습, Validation은 모델·Hyperparameter 선택, Test는
최종 일반화 성능 평가에 사용합니다.

### 상세 설명

Train은 Parameter 학습, Validation은 모델·Hyperparameter 선택, Test는
최종 일반화 성능 평가에 사용합니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> Train은 Parameter 학습, Validation은 모델·Hyperparameter 선택, Test는
> 최종 일반화 성능 평가에 사용합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 275. Cross Validation이란?

### 답변

데이터를 K개 Fold로 나누고 각 Fold를 검증용으로 번갈아 사용해 분할에
따른 우연을 줄이는 평가 방법입니다.

### 상세 설명

데이터를 K개 Fold로 나누고 각 Fold를 검증용으로 번갈아 사용해 분할에
따른 우연을 줄이는 평가 방법입니다. 면접에서는 이 정의에 더해 적용 목적,
장점과 한계, 인접 개념과의 차이를 한 단계 더 설명하는 것이 중요합니다.

### 면접 핵심

> 데이터를 K개 Fold로 나누고 각 Fold를 검증용으로 번갈아 사용해 분할에
> 따른 우연을 줄이는 평가 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 276. Regularization이란?

### 답변

모델이 Training Data에 과도하게 맞는 것을 억제해 일반화 성능을 높이기
위한 방법입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> 모델이 Training Data에 과도하게 맞는 것을 억제해 일반화 성능을 높이기
> 위한 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 277. L1과 L2 Regularization 차이는?

### 답변

L1은 \|w\| penalty로 sparsity를 유도하고 L2는 w² penalty로 큰 Weight를
부드럽게 억제합니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 핵심 수식

$$
L_{L1}=L+\lambda\sum_i|w_i|,\qquad L_{L2}=L+\lambda\sum_iw_i^2
$$

### 면접 핵심

> L1은 \|w\| penalty로 sparsity를 유도하고 L2는 w² penalty로 큰 Weight를
> 부드럽게 억제합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 278. Dropout은 어떻게 Overfitting을 줄이나요?

### 답변

Training 중 일부 unit을 확률적으로 비활성화해 특정 feature나 neuron에
대한 과도한 의존을 줄입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> Training 중 일부 unit을 확률적으로 비활성화해 특정 feature나 neuron에
> 대한 과도한 의존을 줄입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 279. Early Stopping이란?

### 답변

Validation 성능이 더 이상 개선되지 않거나 악화될 때 학습을 중단하고 가장
좋은 시점의 모델을 선택하는 방법입니다.

### 상세 설명

핵심은 Training 성능이 아니라 보지 못한 데이터에 대한
Generalization입니다. Training/Validation 곡선을 함께 보고 모델 복잡도,
Regularization, 데이터 양, Early Stopping을 조절합니다.

### 면접 핵심

> Validation 성능이 더 이상 개선되지 않거나 악화될 때 학습을 중단하고
> 가장 좋은 시점의 모델을 선택하는 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 280. AI 실험에서 Data Leakage란?

### 답변

학습·튜닝 과정에 원래 접근하면 안 되는 Validation/Test 정보가 유입되어
실제보다 성능이 높게 평가되는 문제입니다.

### 상세 설명

연구에서는 개선된 점수보다 그 점수의 원인을 설명할 수 있어야 합니다.
Baseline과 Control Variable을 고정하고 Independent Variable만 바꾸며,
반복 측정·효과 크기·오류 유형을 함께 분석해야 결과의 신뢰성이
높아집니다.

### 면접 핵심

> 학습·튜닝 과정에 원래 접근하면 안 되는 Validation/Test 정보가 유입되어
> 실제보다 성능이 높게 평가되는 문제입니다.

### GomsBook 프로젝트 연결

GomsBook RAG 평가에서는 Golden Dataset을 기준으로 실제 Retrieval과
Answer를 실행하고 Context Precision/Recall, Faithfulness, Answer
Relevancy, NO_ANSWER 등을 분리 평가할 수 있습니다. 연구 단계에서는
개발용 평가셋과 Held-out Test를 분리하는 것이 중요합니다.

### 꼬리질문

**Q. RAG 평가에서도 Data Leakage가 생길 수 있나요?**

네. 같은 Golden Dataset을 보면서 Retriever 설정을 반복 조정한 뒤 그
Dataset의 최종 점수를 일반화 성능처럼 보고하면 평가셋에 과적합될 수
있습니다. 개발용과 Held-out Test를 분리해야 합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 281\~290 상세판

> 주제: Optimization · Gradient

## 281. Gradient Descent란 무엇인가요?

### 답변

Loss의 Gradient 반대 방향으로 Parameter를 반복 업데이트해 Loss를
최소화하는 최적화 방법입니다.

### 상세 설명

Gradient는 Loss가 가장 빠르게 증가하는 방향이므로 그 반대 방향으로
이동합니다. 학습률 η가 너무 크면 최소점을 지나치거나 발산하고, 너무
작으면 수렴이 느립니다.

### 핵심 수식

$$
\theta_{t+1}=\theta_t-\eta\nabla L(\theta_t)
$$

### 면접 핵심

> Loss의 Gradient 반대 방향으로 Parameter를 반복 업데이트해 Loss를
> 최소화하는 최적화 방법입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 282. Batch Gradient Descent, SGD, Mini-batch 차이는?

### 답변

Batch는 전체 데이터, SGD는 한 Sample, Mini-batch는 일부 Sample 묶음으로
한 번의 Gradient를 계산합니다.

### 상세 설명

Gradient는 Loss가 가장 빠르게 증가하는 방향이므로 그 반대 방향으로
이동합니다. 학습률 η가 너무 크면 최소점을 지나치거나 발산하고, 너무
작으면 수렴이 느립니다.

### 면접 핵심

> Batch는 전체 데이터, SGD는 한 Sample, Mini-batch는 일부 Sample
> 묶음으로 한 번의 Gradient를 계산합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 283. Momentum이란 무엇인가요?

### 답변

과거 Gradient 방향을 누적해 진동을 줄이고 일관된 방향의 업데이트를
가속하는 최적화 기법입니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 핵심 수식

$$
v_t=\beta v_{t-1}+(1-\beta)g_t,\qquad \theta_{t+1}=\theta_t-\eta v_t
$$

### 면접 핵심

> 과거 Gradient 방향을 누적해 진동을 줄이고 일관된 방향의 업데이트를
> 가속하는 최적화 기법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 284. Adam Optimizer란 무엇인가요?

### 답변

Gradient의 1차 모멘트와 제곱 Gradient의 2차 raw moment를 추적해
Parameter별 update scale을 적응적으로 조절합니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 핵심 수식

$$
m_t=\beta_1m_{t-1}+(1-\beta_1)g_t,\qquad v_t=\beta_2v_{t-1}+(1-\beta_2)g_t^2
$$

### 면접 핵심

> Gradient의 1차 모멘트와 제곱 Gradient의 2차 raw moment를 추적해
> Parameter별 update scale을 적응적으로 조절합니다.

### 꼬리질문

**Q. Adam과 AdamW는 무엇이 다른가요?**

AdamW는 Weight Decay를 Gradient 기반 update와 분리(decoupled)해
적용합니다. Adaptive optimizer에서 L2 penalty와 Weight Decay가 항상 같은
효과를 내지 않기 때문에 이 구분이 중요합니다.

------------------------------------------------------------------------

## 285. Learning Rate가 왜 중요한가요?

### 답변

한 번의 Parameter Update 크기를 결정하며 너무 크면 발산, 너무 작으면
학습이 지나치게 느릴 수 있습니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 면접 핵심

> 한 번의 Parameter Update 크기를 결정하며 너무 크면 발산, 너무 작으면
> 학습이 지나치게 느릴 수 있습니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 286. Vanishing Gradient란 무엇인가요?

### 답변

역전파에서 여러 미분값을 곱하면서 Gradient가 매우 작아져 앞쪽 Layer의
학습이 어려워지는 현상입니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 면접 핵심

> 역전파에서 여러 미분값을 곱하면서 Gradient가 매우 작아져 앞쪽 Layer의
> 학습이 어려워지는 현상입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 287. Exploding Gradient란 무엇인가요?

### 답변

역전파 과정에서 Gradient가 지나치게 커져 Parameter Update가 불안정해지고
Loss가 발산할 수 있는 현상입니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 면접 핵심

> 역전파 과정에서 Gradient가 지나치게 커져 Parameter Update가
> 불안정해지고 Loss가 발산할 수 있는 현상입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 288. Xavier Initialization이란?

### 답변

fan-in과 fan-out을 고려해 Layer를 통과하는 Activation/Gradient 분산을
안정적으로 유지하려는 Weight 초기화 방법입니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 핵심 수식

$$
\mathrm{Var}(W)\approx\frac{2}{fan_{in}+fan_{out}}
$$

### 면접 핵심

> fan-in과 fan-out을 고려해 Layer를 통과하는 Activation/Gradient 분산을
> 안정적으로 유지하려는 Weight 초기화 방법입니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 289. He Initialization은 Xavier와 무엇이 다른가요?

### 답변

He Initialization은 ReLU 계열을 고려해 대표적으로 Var(W)=2/fan_in을
사용합니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 핵심 수식

$$
\mathrm{Var}(W)\approx\frac{2}{fan_{in}}
$$

### 면접 핵심

> He Initialization은 ReLU 계열을 고려해 대표적으로 Var(W)=2/fan_in을
> 사용합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 290. BatchNorm과 LayerNorm의 차이는?

### 답변

BatchNorm은 batch 통계에 의존하고 LayerNorm은 각 sample/token의 feature
차원을 정규화해 Transformer에 적합합니다.

### 상세 설명

Deep Learning 최적화는 Gradient의 방향뿐 아니라 크기와 안정성이
중요합니다. Initialization, Normalization, Residual Connection,
Optimizer, Learning Rate Schedule, Gradient Clipping이 서로 보완적으로
작동합니다.

### 면접 핵심

> BatchNorm은 batch 통계에 의존하고 LayerNorm은 각 sample/token의
> feature 차원을 정규화해 Transformer에 적합합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

# GomsBook AI Interview 291\~300 상세판

> 주제: Loss · 확률 · Information Theory

## 291. Loss Function이란 무엇인가요?

### 답변

모델의 예측값과 실제 정답 사이의 차이를 수치화하며 학습은 일반적으로
평균 Loss를 최소화합니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\theta^*=\arg\min_\theta\frac{1}{N}\sum_{i=1}^{N}\mathcal{L}(y_i,f_\theta(x_i))
$$

### 면접 핵심

> 모델의 예측값과 실제 정답 사이의 차이를 수치화하며 학습은 일반적으로
> 평균 Loss를 최소화합니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 292. MSE란 무엇인가요?

### 답변

Mean Squared Error로 실제값과 예측값 차이의 제곱 평균이며 주로
Regression에서 사용합니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\mathrm{MSE}=\frac{1}{N}\sum_{i=1}^{N}(y_i-\hat y_i)^2
$$

### 면접 핵심

> Mean Squared Error로 실제값과 예측값 차이의 제곱 평균이며 주로
> Regression에서 사용합니다.

### 꼬리질문

**Q. 이 개념의 한계는 무엇인가요?**

어떤 기법도 모든 데이터와 문제에서 항상 최선은 아닙니다. 적용 가정이
깨지는 상황, 계산 비용, 데이터 요구량, 오류 유형을 확인하고 Baseline과
비교해 선택해야 합니다.

------------------------------------------------------------------------

## 293. Cross Entropy Loss란 무엇인가요?

### 답변

실제 정답 분포와 모델 예측 확률분포의 차이를 평가하며 one-hot 정답에서는
-log p_correct로 단순화됩니다.

### 상세 설명

정답 클래스에 높은 확률을 줄수록 Loss가 작아지고, 정답 확률이 0에
가까워질수록 큰 패널티를 받습니다. Softmax와 함께 쓰면 Logit에 대한
Gradient가 p-y로 단순화되어 분류와 LLM next-token training에 자연스럽게
연결됩니다.

### 핵심 수식

$$
\mathcal{L}_{CE}=-\sum_{i=1}^{C}y_i\log p_i=-\log p_{\mathrm{correct}}\quad(\text{one-hot})
$$

### 면접 핵심

> 실제 정답 분포와 모델 예측 확률분포의 차이를 평가하며 one-hot
> 정답에서는 -log p_correct로 단순화됩니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 294. Binary Classification과 Multi-class Classification 차이는?

### 답변

Binary는 두 class를 구분하며 Sigmoid+BCE를, Multi-class는 상호배타적
여러 class 중 하나를 예측하며 Softmax+CE를 주로 사용합니다.

### 상세 설명

Binary는 두 class를 구분하며 Sigmoid+BCE를, Multi-class는 상호배타적
여러 class 중 하나를 예측하며 Softmax+CE를 주로 사용합니다. 면접에서는
이 정의에 더해 적용 목적, 장점과 한계, 인접 개념과의 차이를 한 단계 더
설명하는 것이 중요합니다.

### 면접 핵심

> Binary는 두 class를 구분하며 Sigmoid+BCE를, Multi-class는 상호배타적
> 여러 class 중 하나를 예측하며 Softmax+CE를 주로 사용합니다.

### 꼬리질문

**Q. 실제 시스템에서는 어떤 기준으로 선택하나요?**

정확도만 보지 않고 데이터 규모, 계산 비용, Latency, 해석 가능성, 변경
빈도, 실패 비용을 함께 고려합니다. 결정론적으로 해결 가능한 부분은
단순한 방법을 우선하고 필요한 부분에만 복잡성을 추가합니다.

------------------------------------------------------------------------

## 295. Softmax란 무엇인가요?

### 답변

여러 Logit을 양수이면서 합이 1인 확률분포로 변환하는 함수입니다.

### 상세 설명

Softmax는 Logit의 상대적 크기를 확률로 바꿉니다. 수치 안정성을 위해 실제
구현에서는 모든 Logit에서 max(z)를 뺀 뒤 exp를 계산하는 방식이
일반적입니다.

### 핵심 수식

$$
p_i=\frac{e^{z_i}}{\sum_j e^{z_j}},\qquad \sum_i p_i=1
$$

### 면접 핵심

> 여러 Logit을 양수이면서 합이 1인 확률분포로 변환하는 함수입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 296. Log-Likelihood란 무엇인가요?

### 답변

관측 데이터에 모델이 부여한 확률의 곱에 log를 적용해 합으로 표현한
값입니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\log L(\theta)=\sum_{i=1}^{N}\log P(y_i\mid x_i;\theta)
$$

### 면접 핵심

> 관측 데이터에 모델이 부여한 확률의 곱에 log를 적용해 합으로 표현한
> 값입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 297. Maximum Likelihood Estimation(MLE)이란?

### 답변

관측된 Training Data가 발생할 확률을 가장 크게 만드는 Parameter를 찾는
추정 방법입니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\theta_{\mathrm{MLE}}=\arg\max_\theta P(D\mid\theta)=\arg\max_\theta\log P(D\mid\theta)
$$

### 면접 핵심

> 관측된 Training Data가 발생할 확률을 가장 크게 만드는 Parameter를 찾는
> 추정 방법입니다.

### 꼬리질문

**Q. MLE와 Cross Entropy는 어떤 관계인가요?**

분류나 next-token prediction에서 정답 데이터의 Log-Likelihood를
최대화하는 것은 Negative Log-Likelihood를 최소화하는 것과 같고, one-hot
target에서는 이것이 Cross Entropy 최소화와 같습니다.

------------------------------------------------------------------------

## 298. Entropy란 무엇인가요?

### 답변

확률분포의 평균적인 불확실성 또는 정보량을 나타내며
H(P)=-ΣP(x)logP(x)입니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
H(P)=-\sum_xP(x)\log P(x)
$$

### 면접 핵심

> 확률분포의 평균적인 불확실성 또는 정보량을 나타내며
> H(P)=-ΣP(x)logP(x)입니다.

### 꼬리질문

**Q. 이 수식이 학습에서 왜 중요한가요?**

수식은 모델의 출력이 어떻게 점수·확률·Loss로 연결되고 그 Loss가 다시
Gradient를 통해 Parameter update로 이어지는지를 설명합니다. 각 항의
의미와 차원을 설명할 수 있어야 합니다.

------------------------------------------------------------------------

## 299. KL Divergence란 무엇인가요?

### 답변

D_KL(P\|\|Q)=ΣP log(P/Q)로 두 확률분포의 차이를 측정하며 비대칭이므로
거리 metric은 아닙니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
D_{KL}(P\parallel Q)=\sum_xP(x)\log\frac{P(x)}{Q(x)}
$$

Cross Entropy와의 관계는 다음과 같습니다.

$$
H(P,Q)=H(P)+D_{KL}(P\parallel Q)
$$

### 면접 핵심

> D_KL(P\|\|Q)=ΣP log(P/Q)로 두 확률분포의 차이를 측정하며 비대칭이므로
> 거리 metric은 아닙니다.

### 꼬리질문

**Q. Cross Entropy와 KL Divergence의 관계는 무엇인가요?**

$H(P,Q)=H(P)+D_{KL}(P\parallel Q)$입니다. 실제 분포 $P$가 고정되어
있으면 $H(P)$는 상수이므로 Cross Entropy를 최소화하는 것은
$D_{KL}(P\parallel Q)$를 최소화하는 것과 연결됩니다.

------------------------------------------------------------------------

## 300. Perplexity란 무엇인가요?

### 답변

언어모델의 평균 Negative Log-Likelihood의 지수로 PPL=exp(NLL_avg)이며
낮을수록 동일 조건에서 다음 Token 예측이 좋음을 뜻합니다.

### 상세 설명

확률 모델 관점에서는 관측 데이터에 높은 확률을 주도록 Parameter를
학습합니다. MLE를 Log 공간에서 보면 Negative Log-Likelihood 최소화가
되고, 분류에서는 Cross Entropy와 연결되며 언어모델에서는 평균 NLL의
지수로 Perplexity를 정의할 수 있습니다.

### 핵심 수식

$$
\mathrm{PPL}=\exp\left(-\frac{1}{N}\sum_{t=1}^{N}\log P(x_t\mid x_{<t})\right)
$$

Cross Entropy/NLL을 $H$라고 쓰면 같은 조건에서 다음처럼 볼 수 있습니다.

$$
\mathrm{PPL}=e^H
$$

### 면접 핵심

> 언어모델의 평균 Negative Log-Likelihood의 지수로 PPL=exp(NLL_avg)이며
> 낮을수록 동일 조건에서 다음 Token 예측이 좋음을 뜻합니다.

### 꼬리질문

**Q. Perplexity가 낮으면 무조건 좋은 LLM인가요?**

아닙니다. Perplexity는 다음 Token 예측 성능을 나타낼 뿐 사실성, 추론,
Instruction Following, RAG Faithfulness를 직접 측정하지 않습니다. 또한
Dataset과 Tokenizer가 다르면 단순 비교가 어렵습니다.

------------------------------------------------------------------------
